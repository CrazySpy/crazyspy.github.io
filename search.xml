<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>卡特兰数的应用</title>
    <url>/2019/09/10/%E5%8D%A1%E7%89%B9%E5%85%B0%E6%95%B0%E7%9A%84%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p>2015年考研有一题：先序遍历为abcd的不同二叉树的个数是（）</p>
<p>对于这一题，先明确：先序遍历和中序遍历确定唯一的二叉树。所以这一题相当于求中序遍历结果有几种，也或者说四个节点的二叉树有几种形状。</p>
<p>这是很常见的一种卡特兰数的应用。一开始书后答案写的是用入栈出栈的方式解释，让我有些摸不着头脑。后来网上搜索了下资料，了解了一下卡特兰数。<br><a id="more"></a><br>卡特兰数的形式是一种递归：</p>
<script type="math/tex; mode=display">
\begin{align}
g(n)
&=g(0) \cdot g(n-1)+g(1) \cdot g(n-2)+...+g(n-1) \cdot g(0) \nonumber\\
&=\sum_{i=1}^{n}g(i-1) \cdot g(n-i) \nonumber\\
&=\frac{C_{2n}^n}{n+1} \nonumber
\end{align}</script><p>它的计算过程这里不去追究，重点对于两种应用进行记录：出栈序列的种数以及n个节点的二叉树形状的种数。</p>
<h1 id="出栈序列的种数"><a href="#出栈序列的种数" class="headerlink" title="出栈序列的种数"></a>出栈序列的种数</h1><p>先考虑如果进栈序列是a1 a2 … an，g(n)为n项序列出栈的种数。如果第一个出栈的项为ak，那么此时出栈的种类就有f(k)=g(k-1)g(n-k)，也就是前k-1项的出栈种数和后n-k项的出栈种数的乘积。</p>
<p>因此可得整个序列的出栈的种数就是：</p>
<script type="math/tex; mode=display">
g(n)=\sum_{i=1}^{n}f(i)=\sum_{i=1}^{n}g(i-1) \cdot g(n-i)</script><p>也就是等于卡特兰数。</p>
<h1 id="n个节点的二叉树形状种类"><a href="#n个节点的二叉树形状种类" class="headerlink" title="n个节点的二叉树形状种类"></a>n个节点的二叉树形状种类</h1><p>对于n个节点的二叉树形状的种类问题，思考方式和出栈种类的思考方式差不多。</p>
<p>考虑一个节点N，以N为根的二叉树有n个节点，有g(n)种组合方式。那么g(n)就是左子树的形状种数和右子树形状种数的乘积。因此类似可得：</p>
<script type="math/tex; mode=display">
g(n)=\sum_{i=1}^{n}g(i-1) \cdot g(n-i)</script><p>因此可得n个节点的树形状的种数就是卡特兰数。</p>
]]></content>
      <categories>
        <category>组合数学</category>
      </categories>
  </entry>
  <entry>
    <title>避免重载全体引用为参数的函数</title>
    <url>/2018/10/22/%E9%81%BF%E5%85%8D%E9%87%8D%E8%BD%BD%E5%85%A8%E4%BD%93%E5%BC%95%E7%94%A8%E4%B8%BA%E5%8F%82%E6%95%B0%E7%9A%84%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>有这样一段代码用于在日志中记录当前的时间和日期，然后添加名字到整体的一个数据结构中:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">multiset</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; names;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">logAndAdd</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span>&amp; name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> now = <span class="built_in">std</span>::chrono::system_clock::now();</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">log</span>(now, <span class="string">"logAndAdd"</span>);</span><br><span class="line">	</span><br><span class="line">	names.emplace(name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>考虑如下的调用之后，你会发现代码的效率并不是很高:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="function"><span class="built_in">string</span> <span class="title">petName</span><span class="params">(<span class="string">"Darla"</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">logAddAdd(petName);</span><br><span class="line"></span><br><span class="line">logAndAdd(<span class="built_in">std</span>::<span class="built_in">string</span>(<span class="string">"Persephone"</span>));</span><br><span class="line"></span><br><span class="line">logAddAdd(<span class="string">"Patty Dog"</span>);</span><br></pre></td></tr></table></figure><br>在第一个调用中，petName被绑定到了引用参数name。在logAndAdd中，name被赋值给了names.emplace函数。因为name是左值，所以name被复制到了names里。这个复制是没有办法避免的，因为name是一个左值引用。<br>在第二个调用中，参数name绑定到了由“Persephone”显式构造的std::string上，这是一个右值。name本身是一个左值，所以被复制到了names中。但是理论上我们可以感觉到，这个name可以移动到names中，因为name引用的是一个右值。<br>在第三个调用中，参数name绑定了一个右值，但是这一次的std::string是隐式从“Patty Dog”构造的。然后就和第二次调用一样，把name复制到names中。因为这一次使用的是直接使用字面的字符串，所以没有必要创造这个临时的std::string，而是应该将这个字面的字符串直接传给emplace函数来构造一个std::string。所以在第三种调用中完全没有必要使用任何的复制和移动std::string的操作。</p>
<p>为了提高上述代码的效率，我们可以使用下面这个使用了全体引用的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt; <span class="function"><span class="keyword">void</span> <span class="title">logAndAdd</span><span class="params">(T&amp;&amp; name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> now = <span class="built_in">std</span>::chrono::system_clock::now();</span><br><span class="line"></span><br><span class="line">	<span class="built_in">log</span>(now, <span class="string">"logAndAdd"</span>); names.emplace(<span class="built_in">std</span>::forward&lt;T&gt;(name));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="function"><span class="built_in">string</span> <span class="title">petName</span><span class="params">(<span class="string">"Darla"</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">logAddAdd(petName);</span><br><span class="line"></span><br><span class="line">logAndAdd(<span class="built_in">std</span>::<span class="built_in">string</span>(<span class="string">"Persephone"</span>));</span><br><span class="line"></span><br><span class="line">logAddAdd(<span class="string">"Patty Dog"</span>);</span><br></pre></td></tr></table></figure><br>效率可以得到提升。</p>
<p>如果这时候重载一个方法可以用id来代替name进行logAndAdd，并且这个id是int类型的，这时候我们如果对logAndAdd函数进行重载，那么就应该是这样的:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="function"><span class="built_in">string</span> <span class="title">nameFormIdx</span><span class="params">(<span class="keyword">int</span> idx)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">logAndAdd</span><span class="params">(<span class="keyword">int</span> idx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> now = <span class="built_in">std</span>::chrono::system_clock::now();</span><br><span class="line"></span><br><span class="line">	<span class="built_in">log</span>(now, <span class="string">"logAndAdd"</span>);</span><br><span class="line"></span><br><span class="line">	names.emplace(nameFromIdx(idx));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="function"><span class="built_in">string</span> <span class="title">petName</span><span class="params">(<span class="string">"Darla"</span>)</span></span>; <span class="comment">//下面三个都调用了logAndAdd(T&amp;&amp;)</span></span><br><span class="line"></span><br><span class="line">logAddAdd(petName);</span><br><span class="line"></span><br><span class="line">logAndAdd(<span class="built_in">std</span>::<span class="built_in">string</span>(<span class="string">"Persephone"</span>));</span><br><span class="line"></span><br><span class="line">logAddAdd(<span class="string">"Patty Dog"</span>);</span><br><span class="line"></span><br><span class="line">logAndAdd(<span class="number">22</span>); <span class="comment">// 调用了logAndAdd(int)</span></span><br></pre></td></tr></table></figure><br>其实这是有问题的。假设有如下的调用:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">short nameIdx;</span><br><span class="line"></span><br><span class="line">logAndAdd(nameIdx); <span class="comment">// 出错！</span></span><br></pre></td></tr></table></figure><br>我们来分析下为什么会出错。首先由两个重载函数，其中一个的参数是全体引用，这个函数的T可以被推导为short。因此编译器就使用这个全体引用为参数的函数，因为它匹配得更加精准了。而int为参数的函数因为short要匹配上它还需要进行到更大范围的类型的转换，因此优先使用精准匹配那个，也就是logAndAdd(T&amp;&amp;)了。但是在这个函数中，name被std::forward到了emplace函数当中，因此在这时，就会执行std::string类型的构造函数，并且参数为short。因为std::string没有这样的构造函数，所以编译失败。这个错误的根源是全体引用为参数的函数因为自动推导是的模板函数变成比其它重载函数有更加准确的类型的参数的函数。参数类型是全体引用的函数是C++中最贪心的函数。它们会推导生成参数类型和实际调用的参数相同的函数。这个导致了重载参数是全体引用的函数是一个不明智的选择。</p>
<p>特别是使用完美转发构造函数(perfect forwarding constructor)时最容易栽进这样的坑里。想想一个Person类，拥有几个构造函数来替代一个只能使用std::string或者index的构造函数:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="comment">// 完美转发构造函数</span></span><br><span class="line">		<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line">			explicit Person(T&amp;&amp; n) : name(std::forward&lt;T&gt;(n))&#123; &#125;</span><br><span class="line"></span><br><span class="line">		explicit Persion(int idx) : name(nameFromIdx(idx))&#123; &#125; </span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>不过还是和前面一样，如上代码中如果传递给logAndAdd是一个整数类型或者其它数字类型，还是调用全体引用函数，从而编译失败。但是更糟糕的是，这里还有很多看不到的编译器自动生成的重载函数。</p>
<p>在前面(<a href="/2018/10/03/理解C++11中特别成员函数的生成/">理解C++11中特别成员函数的生成</a>)讲过，在合适的情况下，编译器将会生成复制和移动构造函数，并且在有模板函数能生成复制或者移动构造函数时仍然会生成。如果Person类的复制构造函数和移动构造函数生成了，Person类将会看上去更加高效:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line">			<span class="function"><span class="keyword">explicit</span> <span class="title">Person</span><span class="params">(T&amp;&amp; n)</span> <span class="comment">// 完美转发构造函数</span></span></span><br><span class="line">			:name(std::forward&lt;T&gt;(n))&#123; &#125;</span><br><span class="line"></span><br><span class="line">		<span class="function"><span class="keyword">explicit</span> <span class="title">Persion</span><span class="params">(<span class="keyword">int</span> idx)</span></span>; <span class="comment">// int为参数的构造函数 </span></span><br><span class="line">		Person(<span class="keyword">const</span> Person&amp; rhs); <span class="comment">// 编译器自动生成的复制构造函数</span></span><br><span class="line">		Person(Person&amp;&amp; rhs); <span class="comment">// 编译器自动生成的移动构造函数</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>如果这样调用:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Person <span class="title">p</span><span class="params">(<span class="string">"Nancy"</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">cloneOfP</span><span class="params">(p)</span></span>; <span class="comment">// 根据p复制新的Person对象，但这不会编译成功</span></span><br></pre></td></tr></table></figure><br>这里我们尝试根据一个Person对象生成另一个Person对象。但是代码不会调用复制构造函数，而是调用完美转发函数。这个函数尝试使用Person对象创建Person中的std::string成员变量，也就是name。因为不存在从Person到std::string的转换方法，因此编译器报错。<br>为什么会出现这样的问题呢？因为这是C++规则规定：<strong>cloneOfP为非const类型的左值，因此这就意味着模板构造函数可以生成一个非const左值为参数的Person构造函数。</strong>也就是Person看上去是这个样子的:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		explicit Person(Person&amp; n) : name(std::forward&lt;Person&amp;&gt;(n));</span><br><span class="line">		<span class="function"><span class="keyword">explicit</span> <span class="title">Persion</span><span class="params">(<span class="keyword">int</span> idx)</span></span>;</span><br><span class="line">		Person(<span class="keyword">const</span> Person&amp; rhs);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">Person <span class="title">p</span><span class="params">(<span class="string">"Nancy"</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">cloneOfP</span><span class="params">(p)</span></span>;</span><br></pre></td></tr></table></figure><br><strong>cloneOfP可能传给复制构造函数或者模板生成的函数。而调用复制构造函数则需要p为const类型的，而模板生成的函数则没有这个要求，所以编译器选择了模板生成的完美转发函数。</strong></p>
<p>当然我们还可以给p一个const属性:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">explicit</span> <span class="title">Person</span><span class="params">(<span class="keyword">const</span> Person&amp; n)</span></span>;</span><br><span class="line">		Person(<span class="keyword">const</span> Person&amp; rhs);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">const</span> Person <span class="title">cp</span><span class="params">(<span class="string">"Nancy"</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">cloneOfP</span><span class="params">(cp)</span></span>; <span class="comment">// 此时就会调用复制构造函数</span></span><br></pre></td></tr></table></figure><br>因为p现在是const类型的，根据选取最佳参数匹配函数的原则应该选参数为const Person&amp;的构造函数。但这时候有两个这样的构造函数，根据C++的规则，我们优先选择非模板生成的函数，也就是选择复制构造函数。</p>
<p>完美转发构造函数和编译器自动生成的复制移动函数经常会导致一系列问题，尤其在继承时。例如在继承类初始化时，复制或移动会导致一些你意想不到的问题:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SpecialPerson</span> :</span> <span class="keyword">public</span> Person</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		SpecialPerson(<span class="keyword">const</span> SpecialPerson&amp; rhs) :Person(rhs) &#123; &#125;</span><br><span class="line">		SpecialPerson(SpecialPerson&amp;&amp; rhs) :Person(<span class="built_in">std</span>::<span class="built_in">move</span>(rhs)) &#123; &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>在上面代码中继承类构造时指定基类的构造方式，并且使用继承类的对象作为基类的构造参数。这种情况下，继承类的复制和移动操作不调用基类的任何移动或者复制构造函数。它们调用的是基类的完美转发构造函数。因为继承类传给基类的是SpecialPerson类型的对象，而基类的不是模板的构造函数都是以Person作为参数类型的。完美转发函数的模板会生成一个完美匹配参数的函数，所以完美转发函数得到调用。</p>
<p>根据上面的讲述，应该可以理解为什么要避免重载全体引用为参数的函数。如果重载参数为全体引用的函数是一个错误的做法，对于需要转发大量参数类型的函数时你该怎么做呢？对于重载参数为全体引用的函数你可以尽量避免，但当无法避免时我们有什么技巧可以使得程序既不会出现上面的问题，又可以达到转发大量参数类型的目的？请参看下一章：<a href="/2018/10/25/重载参数为全体引用的函数的替代方法/">重载参数为全体引用的函数的替代方法</a>。</p>
<p><strong>总结</strong></p>
<ul>
<li>重载参数为全体引用的函数可能会导致重载发生的次数超乎你想象，因为模板函数的参数类型推测结果可能精准匹配你的实参。</li>
<li>完美转发函数尤其可能会导致问题。因为它可以更完美地生成适合非const左值的复制函数，并且可能会劫持继承类调用父类的复制或移动构造函数。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>对于左值引用使用std::move，对于全体引用使用std::forward</title>
    <url>/2018/10/21/%E5%AF%B9%E4%BA%8E%E5%B7%A6%E5%80%BC%E5%BC%95%E7%94%A8%E4%BD%BF%E7%94%A8std_move%EF%BC%8C%E5%AF%B9%E4%BA%8E%E5%85%A8%E4%BD%93%E5%BC%95%E7%94%A8%E4%BD%BF%E7%94%A8std_forward/</url>
    <content><![CDATA[<p>右值引用只会绑定到将要被移动的对象上。对于一个右值引用的参数，可以肯定被引用的对象是要被移动过来的:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		Widget(Widget&amp;&amp; rhs);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>如上面的代码中，你可能想要传递对象给函数调用，为了利用右值的优势你就想要用移动操作。为了你想实现上面的功能，你就需要将绑定的对象转换成右值形式。我们使用std::move来完成这项工作:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		Widget(Widget&amp;&amp; rhs) : name(<span class="built_in">std</span>::<span class="built_in">move</span>(rhs.name)), p(<span class="built_in">std</span>::<span class="built_in">move</span>(rhs.p)) &#123; &#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;SomeDataStructure&gt; p;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>另外，一个全体引用可能被绑定到一个可移动的对象。当右值绑定到全体引用时，全体引用就成了一个右值应用，这时应该使用std::forward:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; <span class="function"><span class="keyword">void</span> <span class="title">setName</span><span class="params">(T&amp;&amp; newName)</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			name = <span class="built_in">std</span>::forward&lt;T&gt;(newName);</span><br><span class="line">		&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>上面的两种情况说明：右值引用的变量通过std::move转发，而全体引用使用的右值通过std::forward转发。<br><a id="more"></a></p>
<p>在<a href="/2018/10/18/理解std_move和std_forward">理解std::move和std::forward</a>中介绍了std::forward对于右值引用可以带来正确的代码，但是会把源码变得繁冗，非常容易导致错误并且不符合语言习惯，也呼吁尽量避免使用std::forward。但是如果这里对全体引用使用std::move，这可能会导致意外地修改左值:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; <span class="function"><span class="keyword">void</span> <span class="title">setName</span><span class="params">(T&amp;&amp; newName)</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			name = <span class="built_in">std</span>::<span class="built_in">move</span>(newName); <span class="comment">// 使用了std::move转换一个全体引用 </span></span><br><span class="line">		&#125;</span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name; <span class="comment">//std::shared_ptr&lt;SomeDataStructure&gt; p;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="function"><span class="built_in">string</span> <span class="title">getWidgetName</span><span class="params">()</span></span>; <span class="comment">// 用于产生Widget的名称</span></span><br><span class="line">Widget w;</span><br><span class="line"><span class="keyword">auto</span> n = getWidgetName();</span><br><span class="line">w.setName(n); <span class="comment">// 本地变量n的值被移动到了w.name中</span></span><br></pre></td></tr></table></figure><br>上面的代码中，因为setName中使用了std::move，本地变量n的数据就被移动进了w.name当中，本地变量变空。</p>
<p>当然你可能认为上面代码不合理，setName不应该声明它的参数为全体引用，或者这样写会使引用不可以是const的，因为setName很明显不应该修改它参数的值。所以你就想到了使用重载来解决上面的两个不足之处:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">setName</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span>&amp; newName)</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			name = newName;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">setName</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span>&amp;&amp; newName)</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			name = <span class="built_in">std</span>::<span class="built_in">move</span>(newName);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> name;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>这样就可以给引用加上const以避免值被修改。这样的确可以运行，但是有很多的不足。首先因为使用了两个函数替代了一个单独的模板，需要编写和维护更多的代码。另外代码效率也非常低下。当如下使用setName时:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">w.setName(<span class="string">"Adela Novak"</span>);</span><br></pre></td></tr></table></figure><br>如果是仅使用了全体引用为参数的setName，模板类型将会推断为const char<em>，字符串“Adela Novak”将会被传递给setName，在setName中将会调用w内部的std::string的赋值操作符。w的数据成员因此会直接从字符串赋值过去，没有任何的临时变量产生。<br>而使用重载的setName就会产生一个临时的std::string对象，用于作为setName的参数，然后这个参数会被移动到w的成员中。因此一个对于setName的调用最终会调用std::string的构造函数(创建临时的std::string)、一次std::string移动赋值操作(把newName移动到w.name中去)、一次std::string的销毁操作(销毁那个临时的std::string)。这是一个比直接调用const char</em>指针需要更加高昂代价的操作。</p>
<p><strong>因此，使用一对对于左值和右值重载的函数替换使用全体引用的模板函数会导致更高昂的运行代价。</strong></p>
<p>使用一对重载函数代替全体引用为参数的模板函数还会导致代码修改变得更加复杂。上面代码中Widget::setName只有一个参数，所以只需要两个重载函数。但是如果函数有更多的参数，每一个都可能是左值或者右值，那么n个参数就需要<script type="math/tex">2^n</script>个重载函数。而且有的函数的参数个数是不定的，例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>, <span class="title">class</span>... <span class="title">Args</span>&gt; <span class="title">shared_ptr</span>&lt;T&gt; <span class="title">make_shared</span>(<span class="title">Args</span>&amp;&amp;... <span class="title">args</span>);</span></span><br></pre></td></tr></table></figure><br>像这样的函数，分左值和右值重载就没有办法了，只能使用全体引用了。</p>
<p>因为std::move会导致原变量边空，而有时候我们希望在一个函数中多次使用被右值引用或者全体引用绑定的对象，这就要使用之前这个对象没有被移动，这时我们就需要对最后一次使用的引用使用std::move或者std::forward，例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setSignText</span><span class="params">(T&amp;&amp; <span class="built_in">text</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	sign.setText(<span class="built_in">text</span>);</span><br><span class="line">	<span class="keyword">auto</span> now = <span class="built_in">std</span>::chrono::system_clock::now();</span><br><span class="line">	signHistory.add(now, <span class="built_in">std</span>::forward&lt;T&gt;(<span class="built_in">text</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>因为我们希望在signHistory.add中使用text，这就要确保setText肯定不会修改text。然后是对全体引用使用了std::forward。</p>
<p>std::move和std::forward类似，但是有时候你可能想使用的是std::move_if_noexcept（具体参看:<a href="/2018/09/30/noexcept关键字的使用">noexcept关键字的使用</a> ）代替std::move。</p>
<p>如果一个函数的返回值是一个值，并且这个值被绑定到了一个右值引用或者一个全体引用，那么请使用std::move或者std::forward返回这个值。例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Matrix <span class="keyword">operator</span>+(Matrix&amp;&amp; lhs, <span class="keyword">const</span> Matrix&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">	lhs += rhs;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">move</span>(lhs); <span class="comment">// 把lhs移动到返回值当中，右值引用使用std::move</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>通过转换lhs为return语句(通过std::move)，lhs将移动到函数的返回值的位置<br>如果忽略调用std::move，也就是代码如下时:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Matrix <span class="keyword">operator</span>+(Matrix&amp;&amp; lhs, <span class="keyword">const</span> Matrix&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">	lhs += rhs;</span><br><span class="line">	<span class="keyword">return</span> lhs;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>lhs因为是左值，编译器将它复制到函数返回值的位置。假设Matrix支持移动构造，在return语句中使用std::move将会使代码效率更高。</p>
<p>如果Matrix不支持移动，这样的转换也不会有什么副作用，因为右值只是会被通过复制构造复制走。如果Matrix之后又支持了移动，“+”操作符将会将会在编译时自动变得更优。在返回右值引用或者全体引用时使用std::move或者std::forward时没有什么坏处，反而只有好处。<br>根据上面的理论，有人就认为：如果对右值引用参数使用std::move可以将复制到函数返回位置变成移动到函数返回位置，那么为什么不能将函数的本地变量也通过移动的方式移动到函数返回位置作为返回呢？例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Widget <span class="title">makeWidget</span><span class="params">()</span> <span class="comment">// “复制”版本的makeWidget</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Widget w;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> w; <span class="comment">// “复制”本地变量w到函数返回位置</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>变成<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Widget <span class="title">makeWidget</span><span class="params">()</span> <span class="comment">// 移动版本的makeWidget</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Widget w;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">move</span>(w); <span class="comment">// 移动本地变量w到函数返回位置（请不要这样做）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>但事实上这样做是有瑕疵的。C++标准委员会已经提前想到了这个问题，委员会在标准中对于本地变量作为返回值时做了特殊的优化，它们通过返回这个本地变量的内存地址对代码进行优化，这样避免了复制到函数返回位置的一次操作。这个优化被称作RVO(return value optimization)，并且是被C++标准明确规定的。<br>这样一个好东西是非常有讲究的，因为你肯定希望在适当的地方使用这个省略复制的操作，并且不会影响到程序的行为。在C++标准中这样规定：同时满足以下两个条件时，编译器可以不复制（或者移动）返回值的函数的返回的本地对象（包括本地变量以及return语句创建的临时对象，但不包括函数的参数）：</p>
<ol>
<li>本地对象的类型和函数返回的本地对象类型一致。</li>
<li>是本地对象作为函数的返回值。</li>
</ol>
<p>所以当我们返回到上面的两种makeWidget中。首先两者都是可行的代码。但是每个正规的C++编译器都会使用RVO来避免对w的复制。这也就是说复制版本的makeWidget并不会在返回的时候复制w。</p>
<p>而移动版本的则会按照预期的去做，将w移动到函数的返回值的位置。那么为什么编译器不会根据RVO不移动呢？答案是它不能这样做。因为上面的条件2中规定了是本地对象作为返回值，而移动版本的makeWidget返回右值引用。因此不可以在这里应用RVO。</p>
<p>当然实际的编码环境复杂，很多情况下你感觉还是需要使用std::move，但即使这样也建议考虑RVO。因为RVO中还这么说，如果编译器没有办法实现忽略复制，那么被返回的对象将会被当做一个右值来对待。标准中RVO如果被使用，将会在复制忽略和隐式调用std::move中二选一。所以下面的代码要不然会忽略复制，要不然就自动添加std::move:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Widget <span class="title">makeWidget</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Widget w;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> w;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>这也就意味着，如果你使用了std::move()作为函数返回值，根据RVO这样不会优化你的代码，反而可能阻碍优化（杜绝了RVO）。</p>
<p>使用std::move在几种情况下可以使代码优化，例如传参的时候，被调用的函数还调用函数并且使用传进来的参数作为将被调用函数的参数。但是在作为返回表达式的一部分时，如果要使用RVO，那么就不建议使用std::move了。</p>
<p><strong>总结</strong></p>
<ul>
<li>在最后一次使用某个引用时，当这个引用是右值引用时使用std::move，是全体引用时使用std::forward。</li>
<li>当某个右值引用或全体引用被函数以返回值的方式返回时，建议对右值引用使用std::move、对全体引用使用std::forward。</li>
<li>不要对本地对象使用std::move或std::forward。因为返回值优化（RVO）会优化它们。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>区别全体引用和右值引用</title>
    <url>/2018/10/20/%E5%8C%BA%E5%88%AB%E5%85%A8%E4%BD%93%E5%BC%95%E7%94%A8%E5%92%8C%E5%8F%B3%E5%80%BC%E5%BC%95%E7%94%A8/</url>
    <content><![CDATA[<p>为了定义一个T类型的右值引用，我们通常使用T&amp;&amp;。但是通常使用了T&amp;&amp;，并不代表你使用了右值引用。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(Widget&amp;&amp; param)</span></span>; <span class="comment">// 右值引用</span></span><br><span class="line">Widget&amp;&amp; var1 = Widget(); <span class="comment">// 右值引用</span></span><br><span class="line"><span class="keyword">auto</span>&amp;&amp; var2 = var1; <span class="comment">// 非右值引用</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt;&amp;&amp; param)</span></span>; <span class="comment">// 右值引用</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(T&amp;&amp; param)</span></span>; <span class="comment">// 非右值引用</span></span><br></pre></td></tr></table></figure><br>事实上，T&amp;&amp;有两层意思。第一层就是普通的右值引用，它绑定了一个右值。另一种是既可能是右值引用，也有可能是左值引用。这种情况下，代码的形式和右值引用很像，都是T&amp;&amp;，但是实际上可能表现得和T&amp;一样。而且，它还可以绑定const和非const对象、volatile和非volatile对象，甚至const和volatile都有的对象。它们可以绑定任何对象。这样的灵活引用值得拥有它独有的名字，我们称之为全体引用(universal reference)。因为它的使用通常伴随着std::forward，所以在部分C++社区中，还称它为forward reference。<br><a id="more"></a></p>
<p>全体引用一般两种情况中出现的比较多，一种是模板函数中形参的类型，还有一种是在auto中出现。这两种情况都是出现在类型推测当中。<strong>如果&amp;&amp;不是出现在类型推测当中，那么就不是全体引用，而是普通的右值引用</strong>:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(Widget&amp;&amp; param)</span></span>; <span class="comment">// 普通右值引用</span></span><br><span class="line">Widget&amp;&amp; var1 = Widget(); <span class="comment">// 普通右值引用</span></span><br></pre></td></tr></table></figure></p>
<p><strong>判断是否是全体引用，是否有类型推测是必须的，但不是充分的。引用的格式必须是严格的T&amp;&amp;的。</strong>在下面的代码中，就不是全体引用:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt;&amp;&amp; param)</span></span>; <span class="comment">// 这是一个右值引用</span></span><br></pre></td></tr></table></figure><br>因为上面的代码没有严格的拥有T&amp;&amp;这种形式，所以上面的代码不是全体引用。</p>
<p>const也会导致引用不是全体引用:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">const</span> T&amp;&amp; param)</span></span>; <span class="comment">// param 是一个右值引用</span></span><br></pre></td></tr></table></figure><br>上面的代码，一是在模板中，二是有严格的T&amp;&amp;格式，你可能觉得它是一个全体引用，但事实上它是一个右值引用。<strong>根据C++11标准，只有非const的&amp;&amp;在模板类型推测时才遵守全体引用的规定。</strong><a href="https://stackoverflow.com/questions/38814939/why-adding-const-makes-the-universal-reference-as-rvalue" target="_blank" rel="noopener">stackoverflow相关解答</a></p>
<p>还有如下的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>, <span class="title">class</span> <span class="title">Allocator</span> = <span class="title">allocator</span>&lt;T&gt;&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">vector</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">push_back</span><span class="params">(T&amp;&amp; x)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>在push_back中它有正确的全体引用的格式，但是它没有类型推测，因为推测的类型在vector初始化的时候就已经决定了。</p>
<p>而vector::emplace_back函数就是右值引用了:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>, <span class="title">class</span> <span class="title">Allocator</span> = <span class="title">allocator</span>&lt;T&gt;&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">vector</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		...</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span>... <span class="title">Args</span>&gt;</span></span><br><span class="line"><span class="class">		<span class="title">void</span> <span class="title">emplace_back</span>(<span class="title">Args</span>... <span class="title">args</span>);</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>这里没有使用T&amp;&amp;的格式，但是它还是全体引用。上面所说的T&amp;&amp;中的T代表的是类型推测的类名，不是一个定式T。</p>
<p>上面还说过auto也可以是全体引用。准确的说，auto&amp;&amp;类型的变量是一个全体引用，因为类型推测发生了并且拥有正确的格式。auto全体引用没有模板函数参数的全体引用使用的那么多，但是它们在C++11标准中相辅相成，在C++14中更是对于双方有重要的互补作用，因为C++14中lambda表达式中可以声明auto&amp;&amp;参数。例如，如果你想要写一个C++14lambda表达式来记录时间，你可以这样做:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> timeFuncInvocation = [](<span class="keyword">auto</span>&amp;&amp; func, <span class="keyword">auto</span>&amp;&amp;... params) </span><br><span class="line">&#123; </span><br><span class="line">	<span class="comment">//start timer; </span></span><br><span class="line">	</span><br><span class="line">	<span class="built_in">std</span>::forward&lt;<span class="keyword">decltype</span>(func)&gt;(func)( <span class="built_in">std</span>::forward&lt;<span class="keyword">decltype</span>(params)&gt;(params)... ); </span><br><span class="line"></span><br><span class="line">	<span class="comment">//stop timer and record elapsed time; </span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>func是一个全体引用，它可以绑定任何可调用的左值或右值对象。params是零个或多个全体引用。因为auto的全体引用，使得timeFuncInvocation可以测量任意函数的执行时间。</p>
<p>最后要说的是，本章讲述的关于全体引用的基础都是抽象概念。隐含的定则被称为引用冲突(reference collapsing)，这个会在后面讲到。当然这个定则不会影响抽象概念的的实用性。区分右值引用和全体引用将有有助于你准确阅读代码，并且避免你与你的合作伙伴写代码时产生二义性。 </p>
<p><strong>总结</strong></p>
<ul>
<li>如果一个函数的模板类型参数中是T&amp;&amp;这样的格式，或者一个对象被声明为auto&amp;&amp;，那么这个参数或者对象是一个全体引用。</li>
<li>如果类型声明不是严格的type&amp;&amp;格式的，或者不是类型推测的，type&amp;&amp;就是一个右值引用而已。</li>
<li>全体引用如果被一个右值初始化，那么它就类似于右值引用。如果被一个左值初始化，那么就类似于左值引用。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>理解std::move和std::forward</title>
    <url>/2018/10/18/%E7%90%86%E8%A7%A3std_move%E5%92%8Cstd_forward/</url>
    <content><![CDATA[<p>std::move不会移动任何东西，std::forward也不会转发任何东西。这两个函数在运行时，甚至不会做任何事。它们没有生成任何可执行的代码，它们的作用会在编译时得到发挥：std::move要做的就是把任意的参数转换成右值。而std::forward在特定的情况下会把参数转换成右值，这种特殊情况其实就是参数也是一个右值。<br><a id="more"></a></p>
<h1 id="std-move"><a href="#std-move" class="headerlink" title="std::move"></a>std::move</h1><p>我们先来看一个std::move在C++11中的大致过程:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> <span class="keyword">typename</span> remove_reference::<span class="function">type&amp;&amp; <span class="title">move</span><span class="params">(T&amp;&amp; param)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">using</span> ReturnType = <span class="keyword">typename</span> remove_reference::type&amp;&amp;;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;ReturnType&gt;(param);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>它引用了一个对象，然后返回了这个对象，上面的remove_reference是模板元编程的一个转换类，它可以把参数的引用符去除。</p>
<p>形参中的&amp;&amp;代表了这是一种全体引用，它不是单纯的右值引用。如果不清楚全体引用，就记住如果T是一个左值引用，那么T&amp;&amp;就是一个左值引用，想进一步了解全体引用，可以参看:<a href="/2018/10/20/区别全体引用和右值引用/">区别全体引用和右值引用</a>。为了防止T&amp;&amp;是一个左值，我们使用了模板元编程的remove_reference来保证返回的是一个右值。</p>
<p>另外，根据我们之前介绍的，上面的代码在C++14中可以这么写:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span> <span class="keyword">typename</span> remove_reference_t&amp;&amp; <span class="title">move</span><span class="params">(T&amp;&amp; param)</span><span class="comment">// 使用C++14标准的remove_reference_t move(T&amp;&amp; param)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">using</span> ReturnType = <span class="keyword">typename</span> <span class="keyword">remove_reference_t</span>&amp;&amp;;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;ReturnType&gt;(param);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看出move函数没有做任何移动的操作，只是做一个转换操作。</p>
<p>事实上，右值只是通常会去被移动，假设有如下的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Annotation</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		explicit Annotation(const std::string text) : value(std::move(text)) &#123; &#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">string</span> value;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>上面的代码如果使用一个std::string类型构造一个Annotation类，会造成value是拷贝构造的。虽然std::move(text)是一个右值，但是由于text是一个const类型，进行了std::move操作之后，变成了右值的const std::string类型。而std::string的构造函数如下:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">string</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="built_in">string</span>(<span class="keyword">const</span> <span class="built_in">string</span>&amp; rhs); <span class="comment">// 复制构造函数</span></span><br><span class="line">		<span class="built_in">string</span>(<span class="built_in">string</span>&amp;&amp; rhs); <span class="comment">// 移动构造函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>value如果选择移动构造函数，被移动的是一个右值的const std::string，因此不可以作为移动构造的参数，所以不可以移动构造。为了维持const的正确性，只能选择复制构造函数进行构造。  </p>
<p>因此我们可以得出两个结论：</p>
<ul>
<li>如果你想对象是可以被别的对象移动构造的，请不要使用const。</li>
<li>std::move不只是做移动操作，它还有可能会导致复制操作。</li>
</ul>
<h1 id="std-forward"><a href="#std-forward" class="headerlink" title="std::forward"></a>std::forward</h1><p><strong>std::forward可以将参数保持左值或者右值。</strong>这也是std::move与std::forward的最大区别之处。std::forward的使用原理这里不介绍，如果有兴趣可以参看:理解引用折叠。下面对其使用举一个例子：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(<span class="keyword">const</span> Widget&amp; lvalArg)</span></span>; <span class="comment">// 参数为左值</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">(Widget&amp;&amp; rvalArg)</span></span>; <span class="comment">// 参数为右值</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">logAndProcess</span><span class="params">(T&amp;&amp; param)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> now = <span class="built_in">std</span>::chrono::system_clock::now(); <span class="comment">// 获取当前时间</span></span><br><span class="line"></span><br><span class="line">	makeLogEntry(<span class="string">"Calling \'process\'"</span>, now); <span class="comment">// 记录日志</span></span><br><span class="line"></span><br><span class="line">	<span class="built_in">process</span>(<span class="built_in">std</span>::forward&lt;T&gt;(param));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Widget w;</span><br><span class="line"></span><br><span class="line">logAndProcess(w);</span><br><span class="line"></span><br><span class="line">logAndProcess(<span class="built_in">std</span>::<span class="built_in">move</span>(w));</span><br></pre></td></tr></table></figure><br>在logAndProcess中，如果logAndProcess的参数为w，那么就会调用参数是左值的process函数；如果logAndProcess的参数为右值，那么就会调用参数是右值的process函数。因为param始终是一个左值，如果直接调用process(param)都会执行参数为左值的重载函数。为了避免这种情况，我们使用std::forward对param进行转换，如果param为右值引用，就会传给process一个右值；如果param为左值引用，那么就不处理，直接将左值传给process。</p>
<p>std::move的转换是无条件的，std::forward的转换是有条件的。这时候可能你会有一个疑问，我们是否可以使用std::forward完全替换std::move？答案是可以的，但是我们没有必要要这样做。因为这两者有它们各自的使用场景。  </p>
<p>我们对比std::forward和std::move的使用后发现，对于std::forward你还需要指定类型，这会导致代码变得繁冗并且容易导致错误。例如:std::forward<T&>和std::forward<T>就是不一样的。  </p>
<p>关于std::move和std::forward的使用条件，可以参看:<a href="/2018/10/21/对于左值引用使用std_move，对于全体引用使用std_forward">对于左值引用使用std::move，对于全体引用使用std::forward</a>。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li>std::move功能是无条件的转换到右值。不过它实际上不移动任何东西。</li>
<li>std::forward只当模板类型参数被绑定为一个右值时才将参数转化到一个右值。</li>
<li>std::move和std::forward在运行时不进行任何操作，它们要做的都是在编译时完成的。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>智能指针中make_xxx函数和new的使用</title>
    <url>/2018/10/14/%E6%99%BA%E8%83%BD%E6%8C%87%E9%92%88%E4%B8%ADmake_xxx%E5%87%BD%E6%95%B0%E5%92%8Cnew%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>在前面的std::unique_ptr、std::shared_ptr智能指针的介绍中，都介绍了使用new的方式构造智能指针。接下来我们会着重将使用new构造智能指针的方式和使用std::make_unique、std::make_share的方式进行比较。<br><a id="more"></a><br>对于std::make_unique函数，这个是C++14标准中的新函数，它的实现大体如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span>... Ts&gt; <span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;T&gt; make_unique(Ts&amp;&amp;... params)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;T&gt;(<span class="keyword">new</span> T(<span class="built_in">std</span>::forward&lt;Ts&gt;(params)...));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="make函数的优势"><a href="#make函数的优势" class="headerlink" title="make函数的优势"></a>make函数的优势</h1><h2 id="make可以少写一次类型"><a href="#make可以少写一次类型" class="headerlink" title="make可以少写一次类型"></a>make可以少写一次类型</h2><p>少写类型最简单的好处就是可以少写代码。其次代码的重复会导致复杂的代码和浮肿的对象代码，并且通常会生成复杂的代码，这些代码会导致包括但不限于代码的无常变化，这种现象通常会导致bug。</p>
<h2 id="使用make可以保证异常安全"><a href="#使用make可以保证异常安全" class="headerlink" title="使用make可以保证异常安全"></a>使用make可以保证异常安全</h2><p>假设有如下的函数定义:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">processWidget</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; spw, <span class="keyword">int</span> priority)</span></span>;</span><br></pre></td></tr></table></figure><br>我们使用new构造std::shared_ptr，这样调用这个函数:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">processWidget(<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt;(<span class="keyword">new</span> Widget), computePriority()); <span class="comment">// 这样可能会导致异常</span></span><br></pre></td></tr></table></figure><br>我们来看一下编译流程:</p>
<ol>
<li>在堆上构造Widget。</li>
<li>构造shared_ptr，参数使用上面在堆上构造的Widget对象。</li>
<li>执行computePriority()。</li>
</ol>
<p>但是记住，编译器没有承诺它会按照如上流程进行编译。也就是说上面的流程只是我们的想象，如果编译器用如下的流程编译，就可能会出现内存泄露:</p>
<ol>
<li>在堆上构造Widget。</li>
<li>执行computePriority()。</li>
<li>构造shared_ptr，参数使用上面在堆上构造的Widget对象。</li>
</ol>
<p>当在第二步的时候，也就是执行computePriority时出现了异常，退出了processWidget函数时，Widget对象就会内存泄露。因为std::shared_ptr不会构造，对第一步的Widget的控制就会消失。<br>所以使用make函数的最重要的原因之一就是保证异常安全。我们可以这样编写上面的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">processWidget(<span class="built_in">std</span>::make_shared&lt;Widget&gt;(), computePriority());</span><br></pre></td></tr></table></figure><br>这样就可以避免了上面可能会出现的内存泄漏。</p>
<h2 id="可以提高代码的效率"><a href="#可以提高代码的效率" class="headerlink" title="可以提高代码的效率"></a>可以提高代码的效率</h2><p>之前讲过，shared_ptr是有两个指针指向两块空间，一个是对象，一个是控制块。如果我们使用了std::make_share构造std::shared_ptr，编译器会自动将代码优化将对象和控制块的空间一起开辟以精简代码并提高效率。如果使用new，对象和控制块很有可能就是分开开辟的，需要两次开辟空间。<br>另外使用make减少了一些在控制块中用于记录的空间，隐含地减少了一些内存空间。同样的std::allocate_shared函数在原理上和std::make_shared类似，因此效率也类似。</p>
<h1 id="make函数的局限性"><a href="#make函数的局限性" class="headerlink" title="make函数的局限性"></a>make函数的局限性</h1><h2 id="无法自定义销毁过程"><a href="#无法自定义销毁过程" class="headerlink" title="无法自定义销毁过程"></a>无法自定义销毁过程</h2><p>unique_ptr和shared_ptr的销毁过程都是通过构造函数实现的，因此如果要自定义销毁过程必须使用构造函数的方式。</p>
<h2 id="无法一步使用initializer-list的方式构造对象"><a href="#无法一步使用initializer-list的方式构造对象" class="headerlink" title="无法一步使用initializer_list的方式构造对象"></a>无法一步使用initializer_list的方式构造对象</h2><p>在make函数中，所有参数都是以括号的方式构造，而不是使用initializer_list方式。如果要使用initializer_list的方法构造对象，只能使用如下的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> initList = &#123;<span class="number">10</span>, <span class="number">20</span>&#125;;</span><br><span class="line"><span class="keyword">auto</span> spv = <span class="built_in">std</span>::make_shared&lt;<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt;(initList);</span><br></pre></td></tr></table></figure></p>
<h1 id="杂项"><a href="#杂项" class="headerlink" title="杂项"></a>杂项</h1><h2 id="不要使用重载的new、delete操作符构造被指向的对象"><a href="#不要使用重载的new、delete操作符构造被指向的对象" class="headerlink" title="不要使用重载的new、delete操作符构造被指向的对象"></a>不要使用重载的new、delete操作符构造被指向的对象</h2><p>new、delete操作符通常用于构造或销毁大小正好是对象的size的对象。这种方法通常不适用于shared_ptr指向的对象的构造和销毁，因为使用std::allocate_shared函数开辟的空间并不是对象的大小，而是对象加上控制块的大小，所以用new、delete操作符以make函数构建或销毁对象是不合理的想法。</p>
<h2 id="std-make-shared构造时的对象和控制块的销毁时间"><a href="#std-make-shared构造时的对象和控制块的销毁时间" class="headerlink" title="std::make_shared构造时的对象和控制块的销毁时间"></a>std::make_shared构造时的对象和控制块的销毁时间</h2><p>使用std::make_shared构造的std::shared_ptr指针，当std::shared_ptr的引用计数器计数为0，那么它就会销毁这个指向的对象。但是在控制块被销毁之前，空间不会释放，这是因为两者的空间是一个整体，只能两者一起销毁。<br>控制块中存放了一些额外信息，这些信息中有一个弱计数器(weak count)是用来记录指向当前对象的std::weak_ptr的数量。当std::weak_ptr检查自身时，首先检查引用计数器是否为0，并以此决定返回指向的对象还是nullptr，这个过程我们在<a href="/2018/10/12/weak_ptr的使用">weak_ptr的使用</a>中也讲过。只要存在std::weak_ptr指向这个对象，这个对象就不能销毁。<strong>因此如果指向的对象是一个十分巨大的对象时，我们可以利用new的方式构造对象，这样可以在引用计数器为0而弱计数器不为0的时候销毁并释放对象的空间，只留下控制块。</strong><br>如果你还关注异常的处理问题时，你可以使用单独语句构造对象，然后把对象作为构造参数的方式来避免这种问题:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; spw(<span class="keyword">new</span> Widget, customDel); <span class="comment">// 单独语句构造对象</span></span><br><span class="line">processWidget(spw, computePriority()); <span class="comment">// 对象作为构造函数的参数</span></span><br></pre></td></tr></table></figure><br>上面代码中，即使spw的构造函数出现了问题，抛出了异常，也会保证customDel可以得到指向。<br>如果这样执行可以得到异常安全的代码，但是在异常不安全的代码中，processWidget的构造函数的第一个参数是右值，因此，它会使用移动的方式移动一个std::shared_ptr。但是使用单独语句构造对象的异常安全的代码的第一个参数是spw，是一个左值，使用的复制的方式，效率上会有损耗，因为我们之前也讲过需要对引用计数器加一操作。为了使代码更高效，我们做如下修改:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; spw(<span class="keyword">new</span> Widget, customDel); <span class="comment">// 单独语句构造对象</span></span><br><span class="line">processWidget(<span class="built_in">std</span>::<span class="built_in">move</span>(spw), computePriority()); <span class="comment">// 对象作为构造函数的参数</span></span><br></pre></td></tr></table></figure></p>
<p>引用《Effective Modern C++》中的一句话：大多数情况下使用make_xxx函数是明智的。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>weak_ptr的使用</title>
    <url>/2018/10/12/weak_ptr%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>前面讲到，指针对对象如果拥有权，我们可以使用unique_ptr；如果指针指向的对象可以分享，那么我们使用shared_ptr。但是在实际上因为shared_ptr对于指向的对象是不知情的，因此就会造成对象已经销毁但是shared_ptr不知情的情况。为了规避这种情况，我们可以使用weak_ptr来判断指向的对象是否已经被销毁。<br><a id="more"></a></p>
<h1 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h1><p>使用示例代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> spw = <span class="built_in">std</span>::make_shared&lt;Widget&gt;(); <span class="comment">// make_shared的用法</span></span><br><span class="line"><span class="built_in">std</span>::weak_ptr&lt;Widget&gt; wpw(spw); <span class="comment">// 初始化weak_ptr，使用shared_ptr作为参数</span></span><br><span class="line"></span><br><span class="line">spw = <span class="literal">nullptr</span>; <span class="comment">// spw指向空指针，那么原来的spw指向的对象就销毁了，也就是wpw现在指向了一个已经销毁的空间</span></span><br><span class="line"><span class="keyword">if</span>(wpw.expired()) <span class="comment">// wpw是否指向了一个销毁的对象</span></span><br><span class="line">&#123; </span><br><span class="line">	...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>如果想让检测是否存在并如果存在就访问指向的对象的操作拥有原子性，我们可以使用lock函数。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; spw1 = wpw.lock(); <span class="comment">// 如果wpw的对象应被销毁，那么就返回空指针(nullptr)。否则就返回wpw指向的对象</span></span><br></pre></td></tr></table></figure></p>
<h1 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h1><p>在《Effective Modern C++》这本书当中，给出了两个使用shared_ptr的例子。一个是缓存池，一个是观察者模式。</p>
<p>缓存池的应用如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;<span class="keyword">const</span> Widget&gt; fastLoadWidget(WidgetID id)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="built_in">std</span>::<span class="built_in">unordered_map</span>&lt;WidgetID, <span class="built_in">std</span>::weak_ptr&lt;<span class="keyword">const</span> Widget&gt;&gt; cache; <span class="comment">// 缓存池，静态类型</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">auto</span> objPtr = cache[id].lock(); <span class="comment">// 如果缓存存在，objPtr就是一个指向缓存的shared_ptr，否则就是指向nullptr</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span>(!objPtr)</span><br><span class="line">	&#123;</span><br><span class="line">		objPtr = loadWidget(id); <span class="comment">// 如果缓存不存在就获取Widget，并且存到缓存中</span></span><br><span class="line">		cache[id] = objPtr;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> objPtr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>上面的例子中，我们cache中存储的是一个weak_ptr。因为考虑到调用者在获取到对缓存的访问后可能会销毁缓存，因此我们使用weak_ptr来判断缓存表中的指针是否指向可用对象。</p>
<p>另外观察者模式中，subject中存放了observer的指针，当subject的状态发生变化，它就会调用observer的相关通知函数，observer是通过指针存放在subject中。而subject对于observer的是否存在可以通过weak_ptr来判断。</p>
<p>对于如下的关系模型，我们也可以使用weak_ptr<br><img src="/images/weak_ptr的使用/1.png" alt=""><br>在上面模型中，A-&gt;B和C-&gt;B都使用了shared_ptr，这时如果B-&gt;A也要一个指针，这个指针的类型改用什么类型呢？<br>我们来讨论：</p>
<ul>
<li>如果使用传统的指针:当A-&gt;B销毁了,这时B中存放的是一个指向A的野指针。</li>
<li>如果使用shared_ptr:当A-&gt;B销毁了，因为还存在C-&gt;B，所以B不会被自动销毁，所以B同样存在一个指向A的野指针。</li>
<li>如果使用weak_ptr:如上面所说，A-&gt;B销毁了，A已经不存在了，这时存在的是一个B-&gt;A的weak_ptr，它因为A不存在而成为了nullptr，所以weak_ptr合适。</li>
</ul>
<h1 id="weak-ptr的控制块"><a href="#weak-ptr的控制块" class="headerlink" title="weak_ptr的控制块"></a>weak_ptr的控制块</h1><p>weak_ptr拥有和shared_ptr一样的控制块，但是weak_ptr不会影响控制块的引用计数，并且还拥有一个弱计数器(weak count)记录了有多少weak_ptr指向了这个对象。通过这两个计数器，我们可以做到判断指向的对象是否被销毁。<br>当一个shared_ptr被销毁，那么引用计数会减一，这时如果引用计数变为0，它就会检查弱计数器是否为0，如果为0，那么就销毁这个对象与控制块。如果不为0，就会不销毁控制块，至于对象是否销毁，我们会在<a href="/智能指针中make_xxx函数和new的使用.html">智能指针中make_xxx函数和new的使用</a>进行讨论。<br>每次使用weak_ptr时，weak_ptr会检查是否引用计数为0，为0那么就会返回nullptr。如果不为0就返回对象。weak_ptr在销毁时会检查引用计数和弱计数器是否同时为0，如果同时为0，那么就销毁控制器（和对象）。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>shared_ptr的使用</title>
    <url>/2018/10/09/shared_ptr%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>前面讲到过unique_ptr的使用，它对指向的对象是有拥有权的，也就是说，unique_ptr被销毁，那么指向的对象也销毁，并且指向的对象不可共享。但是有时候我们需要对指向的对象进行共享，这时候就要用到C++11中另外一个智能指针—shared_ptr了。<br><a id="more"></a></p>
<h1 id="基本介绍"><a href="#基本介绍" class="headerlink" title="基本介绍"></a>基本介绍</h1><p>shared_ptr的原则很简单，就是当指向的对象没有其它shared_ptr需要访问，就把指向的对象销毁。它的实现是依赖于一个引用计数器，记录了当前有多少没有销毁的shared_ptr指向这个对象。因此，shared_ptr的大小和unique_ptr不同，shared_ptr因为这个引用计数器而稍微大一些，准确地说是两倍于普通指针的大小，两个普通指针一个指向被指向的对象，一个指向引用计数器（准确的说应该是控制块Control Block，后面会说）。对于这个引用计数器，当有新的shared_ptr指向了某个对象，则引用计数器就加一。如果某个shared_ptr销毁了，则引用计数器减一，并且检查，是否引用计数器为0，若为0，则代表没有其它的shared_ptr指向这个对象，因此就将这个对象销毁。 另外对于引用计数器的操作是原子的。这确保了在多线程中shared_ptr的线程安全。移动构造的shared_ptr不会引起对应的引用计数器加一，正也因为移动构造的shared_ptr因为不对引用计数器进行加减，所以往往这个操作要快于复制构造的shared_ptr。</p>
<h1 id="自定义销毁过程"><a href="#自定义销毁过程" class="headerlink" title="自定义销毁过程"></a>自定义销毁过程</h1><p>和unique_ptr一样，shared_ptr可以自定义销毁过程。但是shared_ptr不需要将销毁过程的类型作为它自己类型的一部分，也就是不需要在模板参数中定义销毁过程的类型。代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> &#123;</span> &#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">deleteFunc</span><span class="params">(Widget *pw)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">delete</span> pw;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Widget object deleted!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> customDeleter = [](Widget *pw) </span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">delete</span> pw;</span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Widget object deleted!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">	&#125;;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw1(<span class="keyword">new</span> Widget, customDeleter);</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw2(<span class="keyword">new</span> Widget, deleteFunc);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>和unique_ptr不同，不论销毁过程是是函数还是函数对象都不会使shared_ptr变大。这是因为shared_ptr通过两个指针，一个指向被指向的对象，另一个指向一个控制块，这个控制块中存放了引用计数器(reference count)以记录被引用次数、弱计数器(weak count)以及其它的额外数据，这个额外数据就包括了自定义的销毁过程以及空间分配器等。</p>
<h1 id="使用shared-ptr的建议"><a href="#使用shared-ptr的建议" class="headerlink" title="使用shared_ptr的建议"></a>使用shared_ptr的建议</h1><p>shared_ptr不知道是否有其它的shared_ptr指向了和自己一样的对象，所以对于shared_ptr，要知道如下的规则:</p>
<ul>
<li>使用std::make_share是会新建一个控制块。所以使用这个函数的对象一定是要第一次被shared_ptr指向。</li>
<li>从一个对对象有拥有权的指针(如unique_ptr或者auto_ptr)转换成shared_ptr时，是会新建控制块的。因为有拥有权的指针是没有控制块的(unique_ptr可以参见<a href="/2018/10/07/unique_ptr的使用/">unique_ptr的使用</a>)。所以对于构造从这种指针转换来的shared_ptr时，是会新建一个控制块的。</li>
<li>从传统的指针转换来的shared_ptr是有控制块的。如果你想创建一个已经shard_ptr指向已经拥有控制块的对象时，你应该是使用shared_ptr或者weak_ptr来创建，而不是一个传统指针。使用shared_ptr或者weak_ptr构造shared_ptr时，是不会新建控制块的。</li>
</ul>
<p><strong>用一个传统指针构造多个shared_ptr也是非常不好的。</strong>因为这样会构造多个控制块，当其中一个控制块的引用计数器为0时，就会销毁指向的对象，这样其它指向这个的shared_ptr就会成为野指针。这样会导致未定义行为。例如下面的代码，这样做是非常不好的：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> pw = <span class="keyword">new</span> Widget;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw1(pw);</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw2(pw);</span><br></pre></td></tr></table></figure><br>这样做就会导致有一个Widget对象有两个控制块。所以你要避免使用传统的指针构造shared_ptr，使用std::make_shared是一种替代方式，但是使用std::make_shared在需要使用自定义的销毁过程时则没有办法自定义销毁过程。另外还可以使用new构造新的对象作为shared_ptr的构造函数的参数。对于复制shared_ptr也可以使用一个shared_ptr作为构造的参数。</p>
<p>不光传统指针构造shared_ptr可能会导致一个对象多个控制块，对this的不正确使用也可能导致一个对象多个控制块。下面代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt;&gt; processedWidgets;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">()</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			processedWidgets.emplace_back(<span class="keyword">this</span>);</span><br><span class="line">		&#125;</span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> pw1 = <span class="keyword">new</span> Widget;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw2(pw1);</span><br><span class="line">	pw1-&gt;<span class="built_in">process</span>();</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>shared_ptr首先肯定不知道是不是有别的shared_ptr指向要指向的对象，所以每次构造shared_ptr的时候都要新建控制块。在Widget的成员函数process中，它想processedWidgets中插入了一个shared_ptr，指向pw1的对象。然后再main函数中的pw2又以pw1的对象为指向的对象，并且pw2不知道已经在processedWidget中有一个shared_ptr了，这样就导致两个shared_ptr都有各自的控制块，并且都指向同一个对象，所以造成了错误。<br>要解决这样的问题，可以使用std::enable_shared_from_this类，这个类中的shared_from_this方法可以使其继承类完成类似于上述的操作，并且不会造成一个对象多个控制块的问题。代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt;&gt; processedWidgets;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> :</span> <span class="keyword">public</span> <span class="built_in">std</span>::enable_shared_from_this&lt;Widget&gt;</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">()</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			processedWidgets.emplace_back(shared_from_this());</span><br><span class="line">		&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> pw1 = <span class="keyword">new</span> Widget;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw2(pw1);</span><br><span class="line">	pw1-&gt;<span class="built_in">process</span>();</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">``` </span><br><span class="line">这个类中的shared\_from\_this方法会寻找<span class="keyword">this</span>的控制块，如果当前对象的控制块还不存在，那么就会导致未定义行为。所以为了保证不发生这种未定义行为，上述代码我们还会进行改进:</span><br><span class="line">```c++</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt;&gt; processedWidgets;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span>:</span> <span class="keyword">public</span> <span class="built_in">std</span>::enable_shared_from_this&lt;Widget&gt;</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="keyword">static</span> <span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; create() <span class="comment">// factory method</span></span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">auto</span> pw = <span class="keyword">new</span> Widget();</span><br><span class="line"></span><br><span class="line">			<span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt;(pw);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">process</span><span class="params">()</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			processedWidgets.emplace_back(shared_from_this());</span><br><span class="line">		&#125; </span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		Widget() = <span class="keyword">default</span>;</span><br><span class="line">		Widget(<span class="keyword">const</span> Widget&amp;) = <span class="keyword">default</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> pw = Widget::create();</span><br><span class="line"></span><br><span class="line">	pw-&gt;<span class="built_in">process</span>(); </span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; pw2(pw);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>使用工厂方法，强制将新构造的对象在工厂方法中返回一个shared_ptr，使得Widget的对象从诞生起就有shared_ptr指向。这样就不会发生shared_from_this在执行时出现this对象没有控制块导致的未定义行为。</p>
<p>shared_ptr中使用了原子的引用计数器，而控制块比你想象的要复杂得多，例如使用了虚函数保证指向的对象能真正删除等。这些操作看上去都会导致shared_ptr比传统的指针慢很多，但是实际上不用担心，原子的引用计数器操作使用了特殊的机器指令，只要一条指令就能完成对其的操作，对于调用虚函数销毁被指向的对象，这个操作对于一个被指向的对象来说只会执行一次。</p>
<p>当你不知道指针和对象的关系时，使用unique_ptr通常是更好的，因为unique_ptr可以方便的转换成shared_ptr，并且它更像传统的指针。</p>
<p>shared_ptr和unique_ptr有一个很大的区别就是shared_ptr不支持数组操作，它没有[]操作，在默认的销毁过程中没有对数组的delete[]操作。所以建议的方法是使用std::array这类的内建数组代替传统数组。</p>
]]></content>
      <categories>
        <category>C/C++</category>
      </categories>
      <tags>
        <tag>Effective Modern C++</tag>
      </tags>
  </entry>
  <entry>
    <title>unique_ptr的使用</title>
    <url>/2018/10/07/unique_ptr%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>在C++11中，智能指针特性中有一个最方便使用的指针:unique_ptr。这个指针的作用就是用来表示指针对某个对象的拥有权。也就是这个智能指针对象的存亡是和指向对象的存亡是捆绑的。因为这个指针是拥有指向的对象的，当智能指针销毁了，那么它指向的对象也销毁。用户因此也不用考虑动态构造的对象的销毁问题，智能指针已经帮我们包办了。<br>又因为unique_ptr是对于一个对象有拥有权，所以它是没有办法复制的，只能通过移动的方式传递。当一个智能指针将指向的对象移动给另一个智能指针，那么原只能指针就指向了nullptr了。<br>另外，这个unique_ptr，它的大小和传统指针是一样的。它是传统指针的一个包装类(wrapper)，所以它很小巧的。<br><a id="more"></a><br>对于指针对象的销毁，unique_ptr拥有一个默认的销毁方法。但是如果用户想自定义销毁过程，例如在销毁的时候写到日志中一段话，那么就可以使用unique_ptr提供的自定义销毁过程，这个过程可以是函数、lambda表达式以及函数对象等：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> &#123;</span> &#125;;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printDestoryLog</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"Something is destorying!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">delWidgetFunc</span><span class="params">(Widget* w)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	printDestoryLog();</span><br><span class="line">	<span class="keyword">delete</span> w;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> delWidget = [](Widget* w) &#123; </span><br><span class="line">		printDestoryLog();</span><br><span class="line">		<span class="keyword">delete</span> w;</span><br><span class="line">	&#125;;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Widget, <span class="keyword">decltype</span>(delWidget)&gt; ptr1(<span class="literal">nullptr</span>, delWidget); <span class="comment">// 使用lambda表达式作为销毁过程</span></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Widget, <span class="keyword">void</span>(*)(Widget*)&gt; ptr2(<span class="keyword">new</span> Widget, delWidgetFunc); <span class="comment">// 使用函数作为销毁过程</span></span><br><span class="line">	ptr1.reset(<span class="keyword">new</span> Widget); </span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>上面使用了自定义的销毁过程，销毁过程的参数是一个普通的指针。销毁过程中要使用delete关键字销毁普通指针，也就是智能指针类中维护的普通指针。另外在unique_ptr中要说明销毁过程的类型（lambda表达式还是一个函数，函数用函数指针代替），然后在构造时指明销毁过程。<br>《Effective Modern C++》书中的推荐使用lambda表达式作为销毁过程，并且是无捕获的lambda表达式，不光是精简的原因。使用了自定义销毁过程的unique_ptr，往往会比普通指针大一些，通常是1-2个字。对于函数作为销毁过程，unique_ptr的大小取决于这个函数对象中存储了多少状态（类的属性）。而没有捕获的lambda表达式中则没有任何状态，自然也就不会导致unique_ptr的变大。</p>
<p>对于unique_ptr，它还有一个很实用的特点，就是它可以很轻易的转化成shared_ptr，它们之间是可以隐式转换的。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Widget, <span class="keyword">void</span>(*)(Widget*)&gt; widgetFactory()</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;Widget, <span class="keyword">void</span>(*)(Widget*)&gt;(<span class="keyword">new</span> Widget(), delWidgetFunc);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">shared_ptr</span>&lt;Widget&gt; sharedPtr = widgetFactory();</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>理解C++11中特别成员函数的生成</title>
    <url>/2018/10/03/%E7%90%86%E8%A7%A3C++11%E4%B8%AD%E7%89%B9%E5%88%AB%E6%88%90%E5%91%98%E5%87%BD%E6%95%B0%E7%9A%84%E7%94%9F%E6%88%90/</url>
    <content><![CDATA[<p>特别成员函数指的是类中可能会默认生成的函数。在C++98中，它们分别是默认构造函数、默认析构函数、复制操作。C++11添加了重要新特性—移动，所以在C++11中还新添加了移动操作。这几个特别成员函数中，复制操作和移动操作又分成复制构造函数(constructor)、复制等号操作符(assignmeng operator)和移动构造函数、移动等号操作符。<br><a id="more"></a><br>以默认移动构造函数和默认移动等号操作符来举例，两种方法的形式如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">	Widget(Widget&amp;&amp; rhs); <span class="comment">// move constructor</span></span><br><span class="line">	Widget&amp; <span class="keyword">operator</span> = (Widget&amp;&amp; rhs); <span class="comment">// move assignment operator</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br><strong>通常我们认为默认复制构造函数和默认复制等号操作符的两种方法是互相独立的。</strong>也就是说，即使手动定义了构造函数，如果还需要使用复制等号操作符的方式，这时候编译器会自动生成复制等号操作符的方式。这个是C++98中的定义，在C++11中同样延续了这种做法。<br>但是<strong>在移动构造函数和移动等号操作符上，两种方式是不互相独立的。</strong>也就是说，如果你定义了其中一个，但是要用到另外一个时，编译器是不会自动生成另外一个的默认函数。这种做法可以这样理解：因为自定义了其中的一个方式，意味着默认的移动可能会导致问题，所以另一个移动的方式就不能使用默认的。<br>另外，<strong>移动构造函数和移动等号操作符在自定义了复制构造函数或复制等号操作符的类中是不会自动生成的。</strong>这也是可以理解的：因为默认的复制是有问题的，所以有理由判断默认的移动也是有问题的，所以要求自定义移动的函数。同样的，<strong>如果自定义了移动构造函数或移动等号操作符，那么这个类中也不会自动生成复制构造函数或复制等号操作符。</strong>理解方式和上面类似。<br>在理论上还有一个“三条定理”(Rule of Three)。这个三条定理说明了<strong>复制构造函数、复制等号操作符以及析构函数应该一起定义。</strong>因为无论你定义了任何一个复制操作就意味着你可能对另一个复制操作也需要自定义，因为复制操作不是默认的。而且此时析构函数也应该根据他们自定义的复制操作进行自定义的析构操作。 但是这个定理虽然正确，却因为出现的比C++98晚，所以C++98没有采用。而C++11也没有采用这个定理，是因为这样可能会导致老标准的代码升级C++11语法会导致很多问题，为了保证兼容性，也没有采用这套标准。所以我们的代码中应该对C++标准加强，遵守这一定理。 </p>
<p>C++11中特别成员函数的生成规则总结起来如下:</p>
<ul>
<li>默认构造函数：和C++98标准中一样，只有用户没有定义构造函数时(包括复制构造函数和移动构造函数)会自动生成。</li>
<li>析构函数：和C++98标准一样，但有一点：生成的析构函数是默认noexcept的。另外强调，C++98中基类的析构函数如果是virtual的，那么继承类的自动生成的函数也是virtual的。</li>
<li>复制构造函数：和C++98标准一样。在用户没有自定义复制构造函数的时候会进行默认生成，当类中定义了移动的相关操作（对移动等号操作符和移动构造函数中的一个进行自定义），那么就不会自动生成。如果类中存在自定义析构函数或者复制等号操作符时，即使可以自动生成复制构造函数，但也请不要这样使用，这是因为“三条定理”的缘故。</li>
<li>复制等号操作符：和C++98标准一样。在用户没有自定义复制等号操作符的时候会进行默认生成，当类中定义了移动的相关操作（对移动等号操作符和移动构造函数中的一个进行自定义），那么就不会自动生成。如果类中存在自定义析构函数或者复制构造函数时，即使可以自动生成复制等号操作符，但也请不要这样使用，请遵守“三条定理”。</li>
<li>移动构造函数和移动等号操作符：只有当类中没有用户定义的复制的操作、移动的操作或者析构函数时会自动生成。</li>
</ul>
<p>还有一种特殊的情况:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">	<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; Widget(<span class="keyword">const</span> T&amp; rhs);</span><br><span class="line">	<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; Widget&amp; <span class="keyword">operator</span>= (<span class="keyword">const</span> T&amp;rhs);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>当T为Widget类型时，上面的两个函数看上去就是复制操作了，但是这种情况编译器还是会生成默认的复制构造函数和默认的复制等号操作符。这个以后会讨论。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>确保const成员函数线程安全</title>
    <url>/2018/10/02/%E7%A1%AE%E4%BF%9Dconst%E6%88%90%E5%91%98%E5%87%BD%E6%95%B0%E7%BA%BF%E7%A8%8B%E5%AE%89%E5%85%A8/</url>
    <content><![CDATA[<p>在编程中，我们通常会使用这样一种函数：它可以计算某个数值，但是它不修改任何数据，我们很自然的把这种函数定义成const。但是有时候这种函数会进行很复杂的计算，而且因为它可能进行复杂计算，我们通常也都不希望多次运行它，就会使用缓存技术。因为缓存技术的引入，在多线程环境下，就要考虑线程安全问题。<br>C++11中引入了两种用于保证线程安全的类，一个叫做lock_guard，另一个叫做atomic。前者是类似于操作系统中的信号量的PV操作，后者是类似于只能独占访问的变量，使用原子操作进行读写。下面介绍两者。<br><a id="more"></a></p>
<h1 id="lock-guard的使用"><a href="#lock-guard的使用" class="headerlink" title="lock_guard的使用"></a>lock_guard的使用</h1><p>有如下的数据类型，它可能存在于缓存中，当不在缓存中时就需要用户即使获取，为了保证其线程安全，我们使用了如下的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Polynomial</span> </span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">	<span class="keyword">public</span>: </span><br><span class="line">		<span class="keyword">using</span> RootsType = <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">double</span>&gt;; </span><br><span class="line">		<span class="function">RootsType <span class="title">roots</span><span class="params">()</span> <span class="keyword">const</span> </span></span><br><span class="line"><span class="function">		</span>&#123; </span><br><span class="line">			<span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; g(m); </span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span>(!rootsAreValid)</span><br><span class="line">			&#123; </span><br><span class="line">				<span class="comment">// get roots</span></span><br><span class="line">			&#125; </span><br><span class="line"></span><br><span class="line">			<span class="keyword">return</span> rootVals; </span><br><span class="line">		&#125; </span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>: </span><br><span class="line">		<span class="keyword">mutable</span> <span class="built_in">std</span>::mutex m; </span><br><span class="line">		<span class="keyword">mutable</span> <span class="keyword">bool</span> rootsAreValid&#123; <span class="literal">false</span> &#125;; </span><br><span class="line">		<span class="keyword">mutable</span> RootsType rootVals&#123;&#125;; </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>在这种情况下我们可以保证其实线程安全。但是这个类中使用了不可移动的类mutex，所以这个类变得不可复制或移动。</p>
<h1 id="atomic的使用"><a href="#atomic的使用" class="headerlink" title="atomic的使用"></a>atomic的使用</h1><p>有时使用lock_guard保证线程安全会显得大材小用。例如当你想统计对某个数据的访问计数，就可以使用atomic完成，因为其花费的代价可能会比mutex小很多（这个花费要取决于你的标准库是怎么定义mutex）。例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">double</span> <span class="title">distanceFromOrigin</span><span class="params">()</span> <span class="keyword">const</span> <span class="keyword">noexcept</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			++callCount;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">return</span> <span class="built_in">std</span>::hypot(x, y);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="keyword">mutable</span> <span class="built_in">std</span>::atomic&lt;<span class="keyword">unsigned</span>&gt; callCount&#123; <span class="number">0</span> &#125;;</span><br><span class="line">		<span class="keyword">double</span> x, y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>使用如上代码就可以线程安全地统计函数的调用计数了。</p>
<h1 id="选择决策"><a href="#选择决策" class="headerlink" title="选择决策"></a>选择决策</h1><p>刚刚介绍了两个类的使用，下面来介绍什么时候用lock_guard，什么时候用atomic。其实也就是一句话: <strong>单变量的独占访问用atomic，过程或者说是多变量的独占访问使用lock_guard。</strong><br>例如，某个函数会执行一次很复杂的计算，但是这个计算无论何时调用都是一样的结果，也就是说这个函数的返回值是可以进行缓存的。如果这个函数可能会被多线程调用，这个函数该怎么设计？ 根据上面的决策思路，我们会使用lock_guard。因为这里面牵涉到了一系列过程的线程安全。如果我们没有按照上面的决策，我们使用了atomic。那么代码可能是这样的:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">int</span> <span class="title">magicValue</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			<span class="keyword">if</span>(cacheValid)</span><br><span class="line">				<span class="keyword">return</span> cachedValue;</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">			&#123;</span><br><span class="line">				<span class="keyword">auto</span> val1 = expensiveComputation1();</span><br><span class="line">				<span class="keyword">auto</span> val2 = expensiveComputation2();</span><br><span class="line"></span><br><span class="line">				cachedValue = val1 + val2;</span><br><span class="line">				cacheValid = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">				<span class="keyword">return</span> cachedValue;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125; </span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="keyword">mutable</span> <span class="built_in">std</span>::atomic&lt;<span class="keyword">bool</span>&gt; cacheValid&#123; <span class="literal">false</span> &#125;;</span><br><span class="line">		<span class="keyword">mutable</span> <span class="built_in">std</span>::atomic&lt;<span class="keyword">int</span>&gt; cachedValue;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>这段代码使用了atomic，那么它是线程安全的么？如果线程1在计算val1时，线程2进来了，发现数据没有缓存，那么计算val1…如果没有线程执行到cacheValid = true这一句，所有线程执行函数都会执行一遍val1和val2的计算。这样还是造成了很大的资源浪费。需要改进。但如果我把代码的cacheValid置true前移，放在计算val1的前面，那么就不会出现已进入函数，后面线程重复计算的情况，但这时又带来新的问题，就是线程1的数据还没算出来，cacheValid已经标记有缓存，这样接下来的线程不会多计算val1和val2，但是从缓存取出来的数据是有问题的。因此我们就会想到，需要独占的不是说某一个变量，而是几个变量、一个过程。所以我们就要使用lock_guard。代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="function"><span class="keyword">int</span> <span class="title">magicValue</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function">		</span>&#123;</span><br><span class="line">			<span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; guard(m);</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span>(cacheValid)</span><br><span class="line">				<span class="keyword">return</span> cachedValue;</span><br><span class="line">			<span class="keyword">else</span></span><br><span class="line">			&#123;</span><br><span class="line">				<span class="keyword">auto</span> val1 = expensiveComputation1();</span><br><span class="line">				<span class="keyword">auto</span> val2 = expensiveComputation2();</span><br><span class="line"></span><br><span class="line">				cachedValue = val1 + val2;</span><br><span class="line">				cacheValid = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">				<span class="keyword">return</span> cachedValue;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">		<span class="keyword">mutable</span> <span class="built_in">std</span>::mutex m;</span><br><span class="line">		<span class="keyword">mutable</span> <span class="keyword">int</span> cachedValue;</span><br><span class="line">		<span class="keyword">mutable</span> <span class="keyword">bool</span> cacheValid;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>使用这一段代码完美解决了我们的需要。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>constexpr的使用</title>
    <url>/2018/09/30/constexpr%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>C++11中引入了新的关键字constexpr。当用在对象上时，它相当于const的进化。如果用它修饰函数，那么被修饰的函数可以在编译时确定所有值，那么它就在编译时就可以将返回值确定下来，并把返回值定为const类型。然后就可以将函数作为一个const常量使用了。<a id="more"></a>这个在数组的定义等情况有很多用处。例如下面的代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="keyword">int</span> <span class="title">pow</span><span class="params">(<span class="keyword">int</span> base, <span class="keyword">int</span> <span class="built_in">exp</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">array</span>&lt;<span class="keyword">int</span>, <span class="built_in">pow</span>(3, 2)&gt; arr;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>如果在pow函数中，确实可以在编译时推得函数内所有值，那么这个实例确实是可以实现的。</p>
<p>在C++11标准中，这个pow函数要实现有一些麻烦。因为<strong>在C++11标准中，constexpr函数只能拥有一句语句，这句语句也就是return。</strong>为了满足这个要求，只能使用如下代码定义pow函数:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="keyword">int</span> <span class="title">pow</span><span class="params">(<span class="keyword">int</span> base, <span class="keyword">int</span> <span class="built_in">exp</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">exp</span> == <span class="number">0</span> ? <span class="number">1</span> : base * <span class="built_in">pow</span>(base, <span class="built_in">exp</span> - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>这样才能将pow完美定义。</p>
<p>而<strong>在C++14标准中，对于constexpr函数的定义可以更加宽松，constexpr的定义可以是任意多的语句</strong>，但是对于返回值类型还做了要求。例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="keyword">int</span> <span class="title">pow</span><span class="params">(<span class="keyword">int</span> base, <span class="keyword">int</span> <span class="built_in">exp</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> result = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in">exp</span>; ++i)</span><br><span class="line">		result *= base;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> result; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>对于返回值，可以返回字面类型(literal type)，字面类型就是指在编译时可以确定具体指的类型，它是<strong>除了void外的所有内建类型以及部分用户定义的类型也可能是字面类型。</strong>例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span> :</span><br><span class="line">		constexpr Point(double xVal = 0, double yVal = 0) noexcept : x(xVal), y(yVal)&#123;&#125;;</span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">setX</span><span class="params">(<span class="keyword">double</span> newX)</span> <span class="keyword">noexcept</span> </span>&#123;x = newX;&#125;</span><br><span class="line">		<span class="function"><span class="keyword">void</span> <span class="title">setY</span><span class="params">(<span class="keyword">double</span> newY)</span> <span class="keyword">noexcept</span> </span>&#123;y = newY;&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> : </span><br><span class="line">		<span class="keyword">double</span> x;</span><br><span class="line">		<span class="keyword">double</span> y; </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>因为Point的所有成员变量都可以在对象构造的时候进行初始化，所以，这个Point类是一个字面类型。可以在编译的时候的确定，因此就可以作为C++14标准中constexpr函数的返回值。如果一个如实的类型Point的对象被声明为constexpr，那么这个对象就相当于存在于一段只读内存中。<br>在C++11使用了两种限制来确保对象的成员变量不可变，声明为constexpr的对象隐式拥有const的属性，所以setX这类的setter函数是没法执行的，并且成员变量也是没有办法修改的；另外setter函数的void返回类型不是字面类型，所以没有办法执行这一类setter函数。不过在C++14标准中，返回值为void的函数是可以声明为constexpr的:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span> : </span><br><span class="line">		constexpr Point(double xVal = 0, double yVal = 0) noexcept : x(xVal), y(yVal)&#123;&#125;;</span><br><span class="line">		<span class="function"><span class="keyword">constexpr</span> <span class="keyword">void</span> <span class="title">setX</span><span class="params">(<span class="keyword">double</span> newX)</span> <span class="keyword">noexcept</span> </span>&#123;x = newX;&#125; <span class="comment">// C++14通过。C++11报错cannot assign to non-static data member within const member function 'setX'</span></span><br><span class="line">		<span class="function"><span class="keyword">constexpr</span> <span class="keyword">void</span> <span class="title">setY</span><span class="params">(<span class="keyword">double</span> newY)</span> <span class="keyword">noexcept</span> </span>&#123;y = newY;&#125; <span class="comment">// C++14通过。C++11报错</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> : </span><br><span class="line">		<span class="keyword">double</span> x;</span><br><span class="line">		<span class="keyword">double</span> y;</span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">constexpr</span> Point <span class="title">reflection</span><span class="params">(<span class="keyword">const</span> Point&amp; p)</span> <span class="keyword">noexcept</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Point <span class="title">result</span><span class="params">(p)</span></span>;</span><br><span class="line">	result.setX(<span class="number">10.2</span>);</span><br><span class="line">	result.setY(<span class="number">0.1</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">constexpr</span> Point <span class="title">p1</span><span class="params">(<span class="number">9.4</span>, <span class="number">27.7</span>)</span></span>; </span><br><span class="line"><span class="keyword">constexpr</span> <span class="keyword">auto</span> p2 = reflection(p1);</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>noexcept关键字的使用</title>
    <url>/2018/09/30/noexcept%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>在C++11中，引入了noexcept关键字，用来修饰可以保证不会发生异常的函数。<br><a id="more"></a><br>关于noexcept，它可以优化函数。优化器在函数执行结束时，不一定就释放运行栈和所有函数中的对象。因为已经确保了这个函数不会抛出异常，所以优化器认为释放运行栈和对象不会发生异常，也就不用立即释放。如果使用的是C++98的throw()作为函数没有异常的修饰符的话，就不会进行这样的优化。</p>
<p>在C++11中有一个很自然的优化方法，就是将vector的复制改成移动。例如:push_back函数。在C++98中，这个函数会将参数进行复制。而在C++11中，它会在移动时不会出错的情况下进行移动，如果移动可能会出现异常的情况下，它就会采用复制进行。也就是：如果可以移动就移动，不能移动就复制。这样做法的前提：移动不会出错，就是由noexcept来保证的。</p>
<p>如何做到有条件的noexcept呢？请看下面的一段示例:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">void</span> <span class="title">swapEle</span>(<span class="title">T</span> <span class="title">ele1</span>, <span class="title">T</span> <span class="title">ele2</span>) <span class="title">noexcept</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>, <span class="title">size_t</span> <span class="title">N</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">void</span> <span class="title">swap</span>(<span class="title">T</span> (&amp;<span class="title">a</span>)[<span class="title">N</span>], <span class="title">T</span> (&amp;<span class="title">b</span>)[<span class="title">N</span>]) <span class="title">noexcept</span>(<span class="title">noexcept</span>(<span class="title">swapEle</span>(*<span class="title">a</span>, *<span class="title">b</span>)));</span></span><br></pre></td></tr></table></figure><br>这是一段用于交换数组元素的代码。在这段代码使用了有条件的noexcept。<br>在swap函数中，当T为Widget时，如果要判断swap函数是不是noexcept，就要判断先swapEle这个函数当参数是两个指向Widget对象的指针时是不是noexcept来判断swap是不是noexcept。也就是用了这样一种方法，使得push_back等函数可以通过判断参数对象的move函数是不是noexcept来判断移动操作是否可能会导致异常，从而判断push_back使用移动还是复制。</p>
<p>另外还有一种叫做异常中性函数(exception-neutral function)。这种函数它本身不产生异常，而是它要调用的函数可能产生异常，这种异常通过类似于链式的传递，使得调用异常中性函数的函数可能会收到异常。对于这种函数，是不建议使用noexcept的。因为当异常中性函数抛出了来自它正调用的函数的异常时，又根据它的noexcept，导致程序会发生未定义行为。</p>
<p>还有一种叫做宽合约函数(wide contract function)和窄合约函数(narrow contract function)。这两种函数的定义是根据它们的使用范围和参数来定义的。宽合约函数的使用是没有前提条件的，它对参数没有条件，并且永远不会出现未定义行为。这种函数如果不会发生异常，建议使用noexcept。而窄合约函数，则是有前提条件的。比如说，对于某个函数的std::string参数要求长度是1024个字符以下。如果string超过1024，最好的方法是抛出异常，告诉用户string超过了前提。但是如果你使用了noexcept，又不能抛出异常，这时就没有好方法告知用户非法使用了这个函数。所以对于窄合约函数，即使它在前提条件内是非常完美的，也不建议使用noexcept。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>const_iterator与iterator</title>
    <url>/2018/09/29/const_iterator%E4%B8%8Eiterator/</url>
    <content><![CDATA[<p>const_iterator是STL中用来指向const的一个迭代指示器，它指向的元素是不可以进行修改的。但是在C++98中，对const_iterator有些像半成品，不尽人意。不过在C++11中，const_iterator变得更好用了。<br><a id="more"></a><br>在C++98中，我们如果要是用const_iterator进行操作是有些奇怪的。例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator IterT;</span><br><span class="line"><span class="keyword">typedef</span> <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::const_iterator ConstIterT;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; values;</span><br><span class="line"></span><br><span class="line">ConstIterT ci = <span class="built_in">std</span>::<span class="built_in">find</span>( <span class="keyword">static_cast</span>&lt;ConstIterT&gt;(values.<span class="built_in">begin</span>()), <span class="keyword">static_cast</span>&lt;ConstIterT&gt;(values.<span class="built_in">end</span>()), <span class="number">1983</span>);</span><br><span class="line">values.insert(<span class="keyword">static_cast</span>&lt;IterT&gt;(ci), <span class="number">1998</span>);</span><br><span class="line">``` </span><br><span class="line">先不考虑<span class="keyword">static</span>\_cast是否可以转换成功。因为<span class="built_in">vector</span>::insert只接受iterator作为第一个参数，而<span class="built_in">std</span>::<span class="built_in">find</span>参数因为为了ci的数据不可被修改，所以使用了ConstIterT。这样的写法实在太繁琐了。更何况将ConstIterT转换成IterT需要很多麻烦的步骤。 </span><br><span class="line"></span><br><span class="line">而C++<span class="number">11</span>对<span class="keyword">const</span>\_iterator有了很大的改善。上述的代码可以这样写: </span><br><span class="line">```c++</span><br><span class="line"><span class="keyword">auto</span> it = <span class="built_in">std</span>::<span class="built_in">find</span>(values.cbegin(), values.cend(), <span class="number">1983</span>);</span><br><span class="line">values.insert(it, <span class="number">1998</span>);</span><br></pre></td></tr></table></figure><br>我们对于const_iterator的获取非常简单，可以直接使用cbegin、cend、rbegin等。</p>
<p>在C++14中，我们甚至可以使用如下的方法扩展到非STL的C++自带的数组。代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> C, <span class="keyword">typename</span> V&gt; <span class="keyword">decltype</span>(<span class="keyword">auto</span>) findEle(C&amp; container, <span class="keyword">const</span> V&amp; targetVal)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">using</span> <span class="built_in">std</span>::cbegin;</span><br><span class="line">	<span class="keyword">using</span> <span class="built_in">std</span>::cend;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">auto</span> it = <span class="built_in">std</span>::<span class="built_in">find</span>(cbegin(container), cend(container), targetVal);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> it;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> arr[<span class="number">5</span>] = &#123;<span class="number">2000</span>,<span class="number">1983</span>&#125;;</span><br><span class="line">	findEle(arr, <span class="number">1983</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>可以看到这里使用的是C++14的新的非STL成员函数的cbegin和cend，这两个全局函数使得代码更简单。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>override关键字的使用</title>
    <url>/2018/09/28/override%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>在C++11中新引入了关键字override。这个关键字用来声明子类中的方法是重写(override)基类中的相应方法。 <a id="more"></a>在C++98中，发生重写的条件如下:</p>
<ul>
<li>基类的方法必须是virtual</li>
<li>基类函数名和子类函数名必须相同，析构函数除外</li>
<li>基类函数的参数和子类函数的参数必须相同</li>
<li>基类和子类函数的const必须一致</li>
<li>返回类型和异常指示必须相同</li>
</ul>
<p>在C++11中，我们还添加了一个条件：<br><strong>引用修饰词(reference qualifiers)必须一致。</strong>引用修饰词是用来定义当前函数适用于当前对象是左引用还是右引用时，从而选择重写方式。下面举一个例子:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">doWork</span><span class="params">()</span> &amp;</span>; <span class="comment">// 当*this为左值时调用</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">doWork</span><span class="params">()</span> &amp;&amp;</span>; <span class="comment">// 当*this为右值时调用</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">Widget <span class="title">makeWidget</span><span class="params">()</span></span>; <span class="comment">// 一个工厂方法用来生成右值的Widget对象</span></span><br><span class="line"></span><br><span class="line">Widget w;</span><br><span class="line">w.doWork(); <span class="comment">// 调用Widget::doWork&amp;</span></span><br><span class="line"></span><br><span class="line">makeWidget().doWork(); <span class="comment">// 调用Widget::doWork &amp;&amp;</span></span><br></pre></td></tr></table></figure><br>接下来具体回到override的使用上。override的存在是为了防止不满足重写条件的函数被当成重写函数。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">mf1</span><span class="params">()</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mf2</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">int</span> x)</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mf3</span><span class="params">()</span> &amp;&amp;</span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mf4</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Derived</span>:</span> <span class="keyword">public</span> Base</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">public</span>: <span class="function"><span class="keyword">virtual</span> <span class="keyword">float</span> <span class="title">mf1</span><span class="params">()</span> <span class="keyword">override</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mf2</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">int</span> x)</span> <span class="keyword">override</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mf3</span><span class="params">()</span> &amp;&amp; <span class="keyword">override</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">mf4</span><span class="params">()</span> <span class="keyword">const</span> <span class="keyword">override</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>如果Derived的成员方法不满足重写条件，但是编写者认为是重写，并且标注了override关键字，编译器则会进行检查，是否满足重写条件，以此防止意外发生。这也就是override的存在的理由。<br>另外，在C++98中是不存在override这个关键字的，如果某个地方使用了override关键字，例如:变量名、函数名等，但是又将代码改成了C++11风格，这个override是不会影响编译的。因为只有override在作为成员函数修饰词的时候才会被编译器认为是使用了C++11的新关键词，否则就当做一般字符串处理。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>delete关键字的使用</title>
    <url>/2018/09/28/delete%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>在C++98标准中，我们对于会默认产生的方法，例如：默认构造函数等，如果想让它不是默认产生，那么只要把它们的声明放在private，无需定义。<br>例如在istream和ostream不可拷贝，它们的父类basic_ios这样定义:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">charT</span>, <span class="title">class</span> <span class="title">traits</span> = <span class="title">char_traits</span>&lt;charT&gt;&gt; </span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">basic_ios</span>:</span><span class="keyword">public</span> ios_base</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">public</span>: ...</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>:</span><br><span class="line">			basic_ios(<span class="keyword">const</span> basic_ios&amp;); <span class="comment">// not definded</span></span><br><span class="line">			basic_ios&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> basic_ios&amp;); <span class="comment">// not definded</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>在C++11中，我们引入了delete关键字，所以上述代码的C++11形式如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">charT</span>, <span class="title">class</span> <span class="title">traits</span> = <span class="title">char_traits</span>&lt;charT&gt;&gt;</span></span><br><span class="line"><span class="class"><span class="title">class</span> <span class="title">basic_ios</span>:</span><span class="keyword">public</span> ios_base &#123;</span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		basic_ios(<span class="keyword">const</span> basic_ios&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">		basic_ios&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> basic_ios&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>虽然两者写法都不复杂，但是还是推荐使用delete关键字。<br><a id="more"></a><br>两者的区别主要在于以下几点：</p>
<ul>
<li>友元函数如果声明在private中，这个友元函数仍然是有效的。但是如果使用delete，则可以将这个友元函数删除。 </li>
<li>另外对于重载函数，如果实参和形参的数据类型可以隐式转换，我们又不想使用这个隐式转换时，就可以使用delete关键字。例如: <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Operation</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>: <span class="function"><span class="keyword">bool</span> <span class="title">isLucky</span><span class="params">(<span class="keyword">double</span>)</span></span>;</span><br><span class="line">	<span class="function"><span class="keyword">bool</span> <span class="title">isLucky</span><span class="params">(<span class="keyword">int</span>)</span> </span>= <span class="keyword">delete</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Operation op;</span><br><span class="line">	op.isLucky(<span class="number">10</span>); <span class="comment">// error! 参数不可以是一个int</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
如果不声明定义参数为int的isLucky函数，那么参数10会隐式转换成double，然后执行bool isLucky(double)。如果某个成员方法是模板类型的，而我们要对类型推测的某个结果做不编译处理，那么使用private则不能达到目的。只能使用C++11的新特性。例如: <figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Operation</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		<span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; <span class="function"><span class="keyword">void</span> <span class="title">isLucky</span><span class="params">(T* ptr)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;<span class="keyword">void</span> Operation::isLucky&lt;<span class="keyword">void</span>&gt;(<span class="keyword">void</span>*) = <span class="keyword">delete</span>; <span class="comment">// 不可以生成参数为void*的模板函数，而且这句话只能在命名空间内声明，不可以在类空间中声明。</span></span><br></pre></td></tr></table></figure>
要注意，<strong>上面代码的delete只能在命名空间内声明，而不可以在类空间内声明。</strong>对于使用delete的函数，建议是定义在public中。因为编译器在调用一个函数之前，编译器通常是先检查这个函数是在什么访问空间中，如果是private空间中，那么就不管它是不是delete，返回的是private不可直接访问的错误，而不是说这个函数是delete的。这样不利于查错。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>新标准中的enum</title>
    <url>/2018/09/26/%E6%96%B0%E6%A0%87%E5%87%86%E4%B8%AD%E7%9A%84enum/</url>
    <content><![CDATA[<p>在C++11标准中我们引入了enum class，它和C++98标准中的enum一样都是枚举类型，但是还是有很多差别。在这我们就来学习两者之间的差异。<br><a id="more"></a></p>
<h1 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Color &#123; black, white, red &#125;; <span class="comment">// C++98 </span></span><br><span class="line"><span class="keyword">auto</span> white = <span class="literal">false</span>; <span class="comment">// error! redefinition of 'white' </span></span><br><span class="line">``` </span><br><span class="line">```c++</span><br><span class="line"><span class="keyword">enum</span> <span class="class"><span class="keyword">class</span> <span class="title">Color</span> &#123;</span> black, white, red &#125;; <span class="comment">// C++11 </span></span><br><span class="line"><span class="keyword">auto</span> white = <span class="literal">false</span>; <span class="comment">//ok</span></span><br></pre></td></tr></table></figure>
<p>可以看出C++98标准enum中的white作用域是在当前的定义域中，在这个定义域中再次定义white就会出现重定义。而在C++11中，enum class中white的定义域是在这个enum class内，我们在enum class所在定义域中定义white不会出现重定义。</p>
<h1 id="enum接受自动类型转换"><a href="#enum接受自动类型转换" class="headerlink" title="enum接受自动类型转换"></a>enum接受自动类型转换</h1><p>在enum中，成员可以根据需要转换成整数型（在通过整型转换成浮点型等）。而enum class则不是，它的成员是Color类的对象，虽然从二进制角度看成员是存放整型信息的，但是它还是一个Color对象。我们通过下面的代码来理解:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">int</span> param)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; param &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">enum</span> Color&#123; black, white, red &#125;; </span><br><span class="line">	f(white);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>white根据需要转换成了int型。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">int</span> param)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; param &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">enum</span> <span class="class"><span class="keyword">class</span> <span class="title">Color</span>&#123;</span> black, white, red &#125;;</span><br><span class="line">	f(Color::white); <span class="comment">// error!</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这时编译器会报错，我这里的错误是说找不到f的重载函数参数是Color，而且Color也没有办法转换成int类型。也就是说Color::white是以Color对象传参，并且不会进行类型转换。 这时如果执意要使用enum class的成员转换成int，可以使用static_cast进行转换，毕竟它们是二进制相同的。</p>
<h1 id="提前声明"><a href="#提前声明" class="headerlink" title="提前声明"></a>提前声明</h1><p>所谓提前声明就是说变量在某处声明，但是不进行初始化。enum是不支持的，而enum class就是支持的。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> A; <span class="comment">// error!</span></span><br><span class="line"><span class="keyword">enum</span> <span class="class"><span class="keyword">class</span> <span class="title">B</span>;</span> <span class="comment">// ok</span></span><br></pre></td></tr></table></figure><br>导致这样的原因是enum有时会使用紧凑的存储结构，<strong>enum的大小不光由成员的数量决定，还有成员的数据范围决定，因为这个数据范围决定了成员实际使用的内存大小。</strong>当然这只是有时，因为这种紧凑的数据结构可能会牺牲效率，有时为了效率编译器还是会使用非紧凑类型的数据结构。但是这种特别的问题就导致了enum不可以提前声明。<br>解决这种问题的方法是告诉编译器enum的成员都是用什么数据类型。具体代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Color: <span class="built_in">std</span>::<span class="keyword">uint32_t</span>; <span class="comment">//使用32位无符号整型作为一个对象的大小</span></span><br></pre></td></tr></table></figure><br>这样就可以提前声明enum了。<br>虽然enum class默认使用了int作为一个对象的内存大小，但是也可以使用上面的方法修改这一默认值:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="class"><span class="keyword">class</span> <span class="title">Color</span>:</span> <span class="built_in">std</span>::<span class="keyword">uint32_t</span>;</span><br></pre></td></tr></table></figure></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><ul>
<li>C++98的enum是没有作用域的枚举类型，它的成员的作用域是enum的定义范围。而C++11的enum class不是，只能在enum class中可见。</li>
<li>enum的默认成员类型是不确定的，而enum class则使用了int型。</li>
<li>enum class始终可以提前声明，但是enum只有在明确了成员类型才可以提前声明。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>类型别名定义</title>
    <url>/2018/09/25/%E7%B1%BB%E5%9E%8B%E5%88%AB%E5%90%8D%E5%AE%9A%E4%B9%89/</url>
    <content><![CDATA[<p>在C++98中，我们使用typedef来定义一个类型的别名。但是C++11大量的类型推测类型使得typedef不能满足新特性的需求。所以在C++11中使用了using新特性来定义类型的别名。<br><a id="more"></a><br>使用C++11新的using特性定义类型别名，则只要使用如下的代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="keyword">using</span> MyAllocList = <span class="built_in">std</span>::<span class="built_in">list</span>&lt;T, MyAlloc&lt;T&gt;&gt;; </span><br><span class="line">MyAllocList&lt;Widget&gt; lw;</span><br></pre></td></tr></table></figure></p>
<p>而在C++98中我们这样定义别名:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyAllocList</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">typedef</span> <span class="built_in">std</span>::<span class="built_in">list</span>&lt;T, MyAlloc&lt;T&gt;&gt; type;</span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	MyAllocList&lt;Widget&gt;::type lw; </span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>所以在C++98中使用模板的typedef定义的类型别名，都会有::这样的小尾巴，如果使用C++98的定义方式是没有办法避免的。如果使用C++11的标准就可以避免这个小尾巴。<br><strong>在C++98中，模板函数、模板类使用嵌套类或者类中定义typedef的别名时，要使用typename关键字来告诉编译器这是一个“内嵌依赖类型名”，也就是说这个类型是在类内部定义的</strong>:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyAllocList</span> </span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">	<span class="keyword">typedef</span> <span class="built_in">std</span>::<span class="built_in">list</span>&lt;T, MyAlloc&lt;T&gt;&gt; type; </span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> </span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">	<span class="keyword">typename</span> MyAllocList&lt;T&gt;::type <span class="built_in">list</span>; </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>接触过模板元编程的都知道可以使用模板对数据类型进行操作，这是定义在type_traits头文件中的一系列模板完成的，例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::remove_const&lt;T&gt;::type <span class="comment">// C++11: const T -&gt; T</span></span><br></pre></td></tr></table></figure><br>但这种::后缀还是不是很方便，所以在C++14中引入了_t后缀用于去除::后缀。例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::<span class="keyword">remove_const_t</span>&lt;T&gt; <span class="comment">// C++14: const T -&gt; T</span></span><br></pre></td></tr></table></figure></p>
<p>如果你使用的代码是C++98风格的代码，如果想转换成C++14的风格，你只要做类似于下面的事:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; </span></span><br><span class="line"><span class="class"><span class="title">using</span> <span class="title">remove_const_t</span> = <span class="title">typename</span> <span class="title">remove_const</span>&lt;T&gt;:</span>:type;</span><br></pre></td></tr></table></figure></p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>()和{}创建对象的差异</title>
    <url>/2018/09/24/()%E5%92%8C%7B%7D%E5%88%9B%E5%BB%BA%E5%AF%B9%E8%B1%A1%E7%9A%84%E5%B7%AE%E5%BC%82/</url>
    <content><![CDATA[<p>在C++11中，引入了一种新的创建对象方式，{}方法，也就是initializer_list的方式创建对象。这种方法和传统的C++创建对象的方式—()方法有不同之处。<a id="more"></a></p>
<h1 id="的方便之处"><a href="#的方便之处" class="headerlink" title="{}的方便之处"></a>{}的方便之处</h1><p>诚然，这两种方式都是可以接受的，没有明显的优劣之分。但我们在创建对象的时候，使用initializer_list是较方便的。为什么这么说，请看下面的代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> </span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">	<span class="keyword">private</span>: </span><br><span class="line">		<span class="keyword">int</span> x&#123;<span class="number">0</span>&#125;; <span class="comment">// ok </span></span><br><span class="line">		<span class="function"><span class="keyword">int</span> <span class="title">y</span><span class="params">(<span class="number">0</span>)</span></span>; <span class="comment">// error! </span></span><br><span class="line">		<span class="keyword">int</span> z = <span class="number">0</span>; <span class="comment">// ok </span></span><br><span class="line">&#125;; </span><br><span class="line">``` </span><br><span class="line">也就是说在对成员变量初始化中，使用()初始化是不可行的，但是使用=和&#123;&#125;是可行的。</span><br><span class="line">另外：</span><br><span class="line">```c++</span><br><span class="line">class Widget</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">public</span>: </span><br><span class="line">		Widget(<span class="keyword">int</span> x): x(x)&#123;&#125;;</span><br><span class="line">		Widget(<span class="keyword">const</span> Widget&amp; cw) = <span class="keyword">delete</span>; <span class="comment">// Widget is not a copyable class</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span>: </span><br><span class="line">		<span class="keyword">int</span> x; </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="function">Widget <span class="title">w1</span><span class="params">(<span class="number">0</span>)</span></span>; <span class="comment">// ok </span></span><br><span class="line">	Widget w2&#123;<span class="number">0</span>&#125;; <span class="comment">// ok</span></span><br><span class="line">	Widget w3 = <span class="number">0</span>; <span class="comment">// error!</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>对于不可拷贝类对象，使用=进行拷贝构造是不可行的。</p>
<p>综合上面两种情况，只有{}构造方法可以“一劳永逸”，两边都可以使用。</p>
<h1 id="的特殊之处"><a href="#的特殊之处" class="headerlink" title="{}的特殊之处"></a>{}的特殊之处</h1><h2 id="不支持“变窄”方向的初始化"><a href="#不支持“变窄”方向的初始化" class="headerlink" title="不支持“变窄”方向的初始化"></a>不支持“变窄”方向的初始化</h2><p><strong>{}构造的函数，不支持类型向“变窄(narrowing)”的方向自动转换。</strong><br>这个“变窄”可以理解为使数据丢失的方向，比如：double向int类型，double的小数位在转向int的时候会数据丢失。 例如下面的代码就会报错：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> x, y, z;</span><br><span class="line"><span class="keyword">int</span> sum&#123;x + y + z&#125;;</span><br></pre></td></tr></table></figure><br>x+y+z为double，{}的初始化不支持变窄方向的初始化，所以这个int无法初始化。 而()的初始化就支持这种“变窄”的初始化：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">double</span> x, y, z;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(x + y + z)</span></span>;</span><br></pre></td></tr></table></figure></p>
<h2 id="空-的初始化"><a href="#空-的初始化" class="headerlink" title="空{}的初始化"></a>空{}的初始化</h2><p><strong>当初始化使用不带参数的{}时，对象的初始化将会使用无参数的构造函数。</strong>如果想使用空的initializer_list初始化时，使用如下代码:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// both are ok</span></span><br><span class="line"><span class="function">Widget <span class="title">w</span><span class="params">(&#123;&#125;)</span></span>;</span><br><span class="line">Widget w&#123;&#123;&#125;&#125;;</span><br></pre></td></tr></table></figure></p>
<h2 id="构造函数不存在时将会调用-的构造函数"><a href="#构造函数不存在时将会调用-的构造函数" class="headerlink" title="{}构造函数不存在时将会调用()的构造函数"></a>{}构造函数不存在时将会调用()的构造函数</h2><p>例如下面代码中:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>: </span><br><span class="line">		Widget(<span class="keyword">int</span> x, <span class="keyword">double</span> d) &#123;<span class="built_in">cout</span>&lt;&lt; <span class="string">"double"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;; </span><br><span class="line">		Widget(<span class="keyword">int</span> x, <span class="keyword">bool</span> b) &#123;<span class="built_in">cout</span>&lt;&lt; <span class="string">"boolean"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;; </span><br><span class="line">&#125;; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="function">Widget <span class="title">w1</span><span class="params">(<span class="number">1</span>, <span class="literal">true</span>)</span></span>; <span class="comment">// boolean </span></span><br><span class="line">	Widget w2&#123;<span class="number">1</span>, <span class="literal">true</span>&#125;; <span class="comment">// boolean</span></span><br><span class="line">	<span class="function">Widget <span class="title">w3</span><span class="params">(<span class="number">1</span>, <span class="number">5.0</span>)</span></span>;  <span class="comment">// double </span></span><br><span class="line">	Widget w4&#123;<span class="number">1</span>, <span class="number">5.0</span>&#125;;  <span class="comment">// double</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>根据输出结果，我们发现w2选择的构造函数和w1一致，w3和w4一致。所以在不存在{}的构造方法时，{}的构造方法的选择和()一致。</p>
<h2 id="使用-构造优先于使用-构造"><a href="#使用-构造优先于使用-构造" class="headerlink" title="使用{}构造优先于使用()构造"></a>使用{}构造优先于使用()构造</h2><p>上面讨论的是仅存在()的构造方法时。<strong>当{}和()都存在时且排除会“变窄”的构造函数，编译器将会优先选择{}构造方法。</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">		Widget(<span class="keyword">int</span> x, <span class="keyword">double</span> d) &#123;<span class="built_in">cout</span>&lt;&lt; <span class="string">"double"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;;</span><br><span class="line">		Widget(<span class="keyword">int</span> x, <span class="keyword">bool</span> b) &#123;<span class="built_in">cout</span>&lt;&lt; <span class="string">"boolean"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;;</span><br><span class="line">		Widget(<span class="built_in">std</span>::<span class="built_in">initializer_list</span>&lt;<span class="keyword">long</span> <span class="keyword">double</span>&gt; il) &#123;<span class="built_in">cout</span>&lt;&lt; <span class="string">"initializer_list&lt;long double&gt;"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Widget <span class="title">w1</span><span class="params">(<span class="number">1</span>, <span class="literal">true</span>)</span></span>; <span class="comment">// boolean</span></span><br><span class="line">	Widget w2&#123;<span class="number">1</span>, <span class="literal">true</span>&#125;; <span class="comment">// initializer_list&lt;long double&gt;</span></span><br><span class="line">	<span class="function">Widget <span class="title">w3</span><span class="params">(<span class="number">1</span>, <span class="number">5.0</span>)</span></span>;  <span class="comment">// double</span></span><br><span class="line">	Widget w4&#123;<span class="number">1</span>, <span class="number">5.0</span>&#125;;  <span class="comment">// initializer_list&lt;long double&gt;</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>即使w2的最佳构造方法不是{}，w2、w4还是使用了优先调用了{}的构造方法。当然前提是boolean到long double和double到long double不是“变窄”方向。</p>
<p>最后，<strong>当{}的构造函数都不能满足实参时，编译器就会“委曲求全”，使用()方法。</strong>例如:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> </span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="keyword">public</span>:</span><br><span class="line">	Widget(<span class="keyword">int</span> x, <span class="keyword">double</span> d)&#123;<span class="built_in">cout</span> &lt;&lt; <span class="string">"double"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;;</span><br><span class="line">	Widget(<span class="keyword">int</span> x, <span class="keyword">bool</span> b)&#123;<span class="built_in">cout</span> &lt;&lt; <span class="string">"boolean"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;;</span><br><span class="line">	Widget(<span class="built_in">std</span>::<span class="built_in">initializer_list</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; str) &#123;<span class="built_in">cout</span>&lt;&lt; <span class="string">"initializer_list&lt;std::string&gt;"</span> &lt;&lt; <span class="built_in">endl</span>;&#125;; </span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Widget <span class="title">w1</span><span class="params">(<span class="number">1</span>, <span class="literal">true</span>)</span></span>; <span class="comment">// boolean</span></span><br><span class="line">	Widget w2&#123;<span class="number">1</span>, <span class="literal">true</span>&#125;; <span class="comment">// boolean</span></span><br><span class="line">	<span class="function">Widget <span class="title">w3</span><span class="params">(<span class="number">1</span>, <span class="number">5.0</span>)</span></span>;  <span class="comment">// double</span></span><br><span class="line">	Widget w4&#123;<span class="number">1</span>, <span class="number">5.0</span>&#125;;  <span class="comment">// double</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>w2和w4并没有强行使用{}的构造函数。</p>
<h1 id="三-注意事项"><a href="#三-注意事项" class="headerlink" title="三.注意事项"></a>三.注意事项</h1><p>两种初始化方法的结果可能完全不一样，如下代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vector中&#123;&#125;与()功能不一样</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span>... Ts&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doSomeWork</span><span class="params">(Ts&amp;&amp;... params)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	T vec1&#123;<span class="built_in">std</span>::forward&lt;Ts&gt;(params)...&#125;; <span class="comment">// use &#123;&#125;</span></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"&#123;&#125; constructor : "</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; (<span class="keyword">int</span>)vec1.<span class="built_in">size</span>(); ++i) </span><br><span class="line">	&#123; </span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; vec1[i] &lt;&lt; <span class="string">' '</span>; </span><br><span class="line">	&#125; </span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="function">T <span class="title">vec2</span><span class="params">(<span class="built_in">std</span>::forward&lt;Ts&gt;(params)...)</span></span>; <span class="comment">// use () </span></span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"() constructor : "</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>; </span><br><span class="line">	</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; (<span class="keyword">int</span>)vec2.<span class="built_in">size</span>(); ++i) </span><br><span class="line">	&#123; </span><br><span class="line">		<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; vec2\[i\] &lt;&lt; <span class="string">' '</span>; </span><br><span class="line">	&#125; </span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	doSomeWork&lt;<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt;(<span class="number">10</span>, <span class="number">20</span>); </span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="四-总结"><a href="#四-总结" class="headerlink" title="四.总结"></a>四.总结</h1><ul>
<li>使用{}的初始化方法是最被广为接受的格式，而且它可以防止初始化参数的“变窄”转换。</li>
<li>{}初始化方法尽可能使用initializer_list的初始化方法，如果实在没有可以使用的{}初始化方法，才去考虑()初始化方法。即使()构造方法的参数和实参很符合，它也不优先考虑。</li>
<li>{}初始化方法和()初始化方法会导致两个对象结果不一致。例如:std::vector<int>的两个初始化方法意义完全不一样。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
      </categories>
      <tags>
        <tag>Effective Modern C++</tag>
      </tags>
  </entry>
  <entry>
    <title>关于auto的一些补充说明</title>
    <url>/2018/09/17/%E5%85%B3%E4%BA%8Eauto%E7%9A%84%E4%B8%80%E4%BA%9B%E8%A1%A5%E5%85%85%E8%AF%B4%E6%98%8E/</url>
    <content><![CDATA[<p>之前讲过，auto的作用是能推导变量、函数的类型（可以参考:<a href="/2018/09/15/auto类型的推测准则">auto类型的推测准则</a>）。然而在auto的实际使用中，有一个点是需要注意的<a id="more"></a>，我们先来看下面的一个例子： </p>
<pre><code class="lang-c++">vector&lt;bool&gt; getPriorities() 
{ 
    vector&lt;bool&gt; s; 

    s.push_back(true);
    s.push_back(false); 
    s.push_back(true); 
    s.push_back(true); 
    s.push_back(false); 
    s.push_back(true); 

    return s; 
} 

template&lt;typename T&gt; 
void showPriority(T priority) 
{ 
    if(priority) 
        cout &lt;&lt; &quot;true&quot; &lt;&lt; endl; 
    else 
        cout &lt;&lt; &quot;false&quot; &lt;&lt;endl; 
} 

int main() 
{ 
    auto priority1 = getPriorities()[0]; 
    bool priority2 = getPriorities()[0]; 

    showPriority(priority1); 
    showPriority(priority2); 

    return 0; 
}
</code></pre>
<p>上面的例子的运行结果是不稳定的，因为其中包含了未定义行为，也就是调用showPriority(priority)的行为是未定义行为。 这是为什么？ </p>
<p>首先我们要知道vector的[]操作符的定义。<br>与其它的vector的[]操作符不同，其它的都会返回T&amp;，而对于T为bool类型，vector的[]操作符不会bool&amp;，而是返回vector::reference类型，这是个嵌套类，也就是reference是定义在vector中的一个类。我推测这样的做法是因为bool在C++中有效数据为仅1位，但是却需要1字节的空间，而bool在vector内部的存储是使用bit作为单位，reference的作用就是无缝对接1bit的bool和1byte的bool，使用户看上去两者一样，也就是对用户透明。还有很肯定的是这个reference可以自动和bool进行类型隐式转换（毕竟要做到对用户透明）。并且这个reference有一个成员指针，用来指向reference内部的表示bool结果的某一位数据。</p>
<p>上面的当priority为auto类型并在进行=操作时，因为自动推测类型，实际上赋值过来的是这个reference。当在showPriority中进行调用时，根据reference的类型隐式转换，这个reference内部的指针指向的某一位就是需要的结果。但是指向的位实际上已经销毁了，因为getPriorities()函数返回的vector数据的生命周期只在赋值的那句话里。<br>简单来说，导致这个未定义的原因实际上还是一个浅拷贝对象带来的问题。priority1存储的只是一个对象的浅拷贝副本，并且被拷贝对象在执行完赋值语句后就被销毁了，因此这个副本就存放了野指针，从而导致showPriority的实参其实是从一个野指针指向的位转化而来的bool型。 反观priority2，则在reference内部指针指向的空间没有被销毁前转化成了bool数据类型，从而没有导致未定义行为。 在《effective moderen C++》这本书中还因此引申出了proxy类，因为proxy类都是利用了这种指针指向被代理数据，而当被指向的空间被不限于作用域的问题被销毁后，这个proxy类对象中的指针就成了野指针，从而造成了一系列问题。我认为上面的问题不是proxy和auto的水土不和，而是没有意识到浅拷贝。这是我对这本书这一章的一个加深理解。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>C++如何查看自动推测类型的结果</title>
    <url>/2018/09/17/C++%E5%A6%82%E4%BD%95%E6%9F%A5%E7%9C%8B%E8%87%AA%E5%8A%A8%E6%8E%A8%E6%B5%8B%E7%B1%BB%E5%9E%8B%E7%9A%84%E7%BB%93%E6%9E%9C/</url>
    <content><![CDATA[<p>由于实际编程时的各种复杂情况，即使C++的自动类型推测有许多的规则，但是还是会出现编程人员无法正确推测类型，这时候有几种方法可以用于查看推测结果。<br><a id="more"></a></p>
<h1 id="编译器推测"><a href="#编译器推测" class="headerlink" title="编译器推测"></a>编译器推测</h1><p>这种方法是利用错误输出（一般是声明模板函数但不定义）来达到查看推测结果的目的。 </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TD</span>;</span> </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">auto</span> x = <span class="number">10</span>; </span><br><span class="line">	TD&lt;<span class="keyword">decltype</span>(x)&gt;(); </span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line">``` </span><br><span class="line">这样我们在TD类未定义的错误中，可以看到x的推测类型。 </span><br><span class="line"></span><br><span class="line">我这里使用的是Clang++编译器，它的错误提示是： error: implicit instantiation of undefined template 'TD' 这样可以看到x的类型为int</span><br><span class="line"></span><br><span class="line"># 运行时的输出</span><br><span class="line"></span><br><span class="line">上面的方法是类似于利用错误输出结果，下面介绍的方法可以运行时输出推测结果。</span><br><span class="line"></span><br><span class="line">## C++标准库自带</span><br><span class="line"></span><br><span class="line">这里利用<span class="built_in">std</span>::<span class="keyword">typeid</span>运算符和<span class="built_in">std</span>::type\_info::name方法。</span><br><span class="line"></span><br><span class="line"><span class="keyword">typeid</span>运算符返回<span class="keyword">const</span> type\_info&amp;类型。通过type_info的成员方法name我们可以获得一个C风格字符串，这个字符串蕴含了运算对象类型。 </span><br><span class="line"></span><br><span class="line">使用例子： </span><br><span class="line">```c++ </span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">const</span> T* param)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"T = "</span> &lt;&lt; <span class="keyword">typeid</span>(T).name() &lt;&lt; <span class="string">'\n'</span>; </span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"param = "</span> &lt;&lt; <span class="keyword">typeid</span>(param).name() &lt;&lt; <span class="string">'\n'</span>; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">auto</span> x = <span class="number">1</span>; </span><br><span class="line">	f(&amp;x); </span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line">``` </span><br><span class="line">如果使用的是Microsoft的编译器，这个字符串就很舒服，直接显示了运算对象的类型。上面的运行结果应该是： T = <span class="keyword">int</span> param = <span class="keyword">int</span> <span class="keyword">const</span>\* 。如果和我一样使用的clang或者GNU的GCC编译器，那么就会显示 T = i param = PKi 其中PK代表pointer to konst, i代表<span class="keyword">int</span>。 </span><br><span class="line">但是这种使用C++标准库自带的<span class="keyword">typeid</span>运算符的方法并不可靠，例如： </span><br><span class="line">```c++ </span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="keyword">void</span> f2(<span class="keyword">const</span> T&amp; param) </span><br><span class="line">&#123; </span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"T = "</span> &lt;&lt; <span class="keyword">typeid</span>(T).name() &lt;&lt; <span class="string">'\n'</span>; </span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; <span class="string">"param = "</span> &lt;&lt; <span class="keyword">typeid</span>(param).name() &lt;&lt; <span class="string">'\n'</span>; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">auto</span> x = <span class="number">1</span>; </span><br><span class="line">	f2(x); </span><br><span class="line">	</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line">``` </span><br><span class="line">返回显示T和param均为<span class="keyword">int</span>，而实际上param为<span class="keyword">const</span> <span class="keyword">int</span>&amp;类型。 这个是在<span class="keyword">typeid</span>的运算符中定义能找到原因的。<span class="keyword">typeid</span>的定义是返回运算对象使用通过传值的方式传给<span class="keyword">template</span>函数推测出来的参数的值。上面的代码中就是相当于<span class="keyword">typeid</span>(param)返回的是如下代码推测的结果： </span><br><span class="line">```c++</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f3</span><span class="params">(T param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> x = <span class="number">1</span>;</span><br><span class="line">	f3(x);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>所以使用typeid运算符并不能在大多数情况下返回你想要的结果</p>
<p>所以接下来使用的是一种利用C++的扩展库boost中的方法来完成运行时输出推测结果的任务。</p>
<h2 id="boost库完成"><a href="#boost库完成" class="headerlink" title="boost库完成"></a>boost库完成</h2><hr>
<p>我们可以利用boost库中的type_id_with_cvr来完成上面的任务：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">const</span> T param)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"T = "</span> &lt;&lt; boost::typeindex::type_id_with_cvr&lt;T&gt;().pretty_name() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">	<span class="built_in">std</span>::<span class="built_in">cout</span>&lt;&lt;<span class="string">"param = "</span> &lt;&lt; boost::typeindex::type_id_with_cvr&lt;<span class="keyword">decltype</span>(param)&gt;().pretty_name() &lt;&lt;<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> x = <span class="number">1</span>;</span><br><span class="line">	f(x);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>结果是 T = int param = int const 非常漂亮，和使用Microsoft编译器的结果是一样的，可以直接显示出类型，而不是使用PK这类的缩写。</p>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>decltype的运用</title>
    <url>/2018/09/16/decltype%E7%9A%84%E8%BF%90%E7%94%A8/</url>
    <content><![CDATA[<p>之前我们讲到过，在C++11标准中，auto不可以直接用于函数的返回值以及lambda表达式的参数类型。而在C++14标准中，对这一规定进行了变更。</p>
<p>C++11中不是不可以使用auto返回类型的模板函数，而是需要使用decltype的方式进行尾置类型定义。具体的形式如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; <span class="title">auto</span> <span class="title">foo</span>(<span class="title">T</span> <span class="title">param</span>) -&gt; <span class="title">decltype</span>(<span class="title">param</span>) &#123;</span> <span class="keyword">return</span> param; &#125;</span><br></pre></td></tr></table></figure><br>它使用了“-&gt;decltype(param)”的形式对使用auto返回类型的函数定义了返回值的类型—返回的是param的类型。但是这种写法可能是因为其不像C++的语法，在C++14中使用了decltype(auto)的写法进行代替。下面就来介绍decltype在C++中的使用。<br><a id="more"></a></p>
<h1 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h1><p>decltype的用法很简单：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> x = <span class="number">0</span>; <span class="comment">//decltype(x) is int</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">f</span><span class="params">(<span class="keyword">const</span> Widget&amp; w)</span></span>; <span class="comment">//decltype(f) is bool(const Widget&amp;), decltype(w) is const Widget&amp; </span></span><br><span class="line">``` </span><br><span class="line">**<span class="keyword">decltype</span>几乎可以正确返回变量或者表达式的正确数据类型。** 但是存在一个例外： <span class="keyword">decltype</span>对于不是使用名字的参数，并且这个参数是左值的情况是返回引用类型。 例如：如果<span class="keyword">decltype</span>中的参数使用的名字，并且这个名字被括号包围（不是<span class="keyword">decltype</span>的括号），则认定参数为左值，且为不是使用名字的参数，所以返回引用类型。下面的代码可以得出这样的结论： </span><br><span class="line">```c++ </span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; </span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TD</span>;</span> </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="keyword">int</span> x = <span class="number">10</span>; </span><br><span class="line">	TD&lt;<span class="keyword">decltype</span>((x))&gt; td; </span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125; </span><br><span class="line">``` </span><br><span class="line">这里编译器返回的错误告知我们td推测的T的类型是<span class="keyword">int</span>&amp;，也就是引用类型。</span><br><span class="line"></span><br><span class="line"><span class="meta"># decltype(auto)的使用</span></span><br><span class="line">在C++<span class="number">11</span>中，如果函数返回类型要使用<span class="keyword">auto</span>是，则必须使用尾置返回类型。这种语法非常不像C++的语法，所以在C++<span class="number">14</span>中，使用了新的东西对其进行替代--<span class="keyword">decltype</span>(<span class="keyword">auto</span>)</span><br><span class="line"></span><br><span class="line">在对函数的返回类型进行推测时，用法如下： </span><br><span class="line">```c++ </span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt; <span class="keyword">decltype</span>(<span class="keyword">auto</span>) </span><br><span class="line">f(T param) </span><br><span class="line">&#123; </span><br><span class="line">	<span class="keyword">return</span> param; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> x = <span class="number">0</span>; </span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;f(x)&lt;&lt;<span class="built_in">endl</span>; </span><br><span class="line">&#125;</span><br><span class="line">``` </span><br><span class="line">它和作用和使用尾置返回类型是一样的。</span><br><span class="line"></span><br><span class="line">而在对于变量的类型推测时，也是可以使用<span class="keyword">decltype</span>(<span class="keyword">auto</span>)的。</span><br><span class="line">```c++</span><br><span class="line">Widget w;</span><br><span class="line"><span class="keyword">const</span> Widget&amp; cw = w; </span><br><span class="line"><span class="keyword">auto</span> myWidget1 = cw; <span class="comment">// myWidget1's type is Widget </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">decltype</span>(<span class="keyword">auto</span>) myWidget2 = cw; <span class="comment">// myWidget2's type is const Widget&amp;</span></span><br></pre></td></tr></table></figure></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><ol>
<li>decltype几乎可以返回所有类型的正确类型。</li>
<li>decltype在参数为左值类型、没有使用名称时，会返回引用类型，例如：decltype((x));</li>
<li>C++14支持decltype(auto)的使用，用法和auto很像，但是它的推测规则是使用decltype的规则。</li>
</ol>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>auto类型的推测准则</title>
    <url>/2018/09/15/auto%E7%B1%BB%E5%9E%8B%E7%9A%84%E6%8E%A8%E6%B5%8B%E5%87%86%E5%88%99/</url>
    <content><![CDATA[<p>auto是C++11标准新引入的一个关键字，用于类型推测。</p>
<p>例如：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = <span class="number">10</span>;</span><br></pre></td></tr></table></figure></p>
<p>编译器则会在编译时自动将x调整为int类型。</p>
<p>关于auto类型的调整类型的规则，它还是遵循C++11中template的规则来进行的，稍有不同。关于C++11中template的类型推测可以参见:<a href="/2018/09/14/C++11标准的模板类型推测">C++11标准的模板类型推测</a><br><a id="more"></a></p>
<h1 id="auto对于变量的类型推测"><a href="#auto对于变量的类型推测" class="headerlink" title="auto对于变量的类型推测"></a>auto对于变量的类型推测</h1><p>例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = <span class="number">27</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">auto</span> cx = x;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">auto</span>&amp; rx = x;</span><br></pre></td></tr></table></figure>
<p>上面的代码的类型推测和下面代码的推测过程类似。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func_for_x</span><span class="params">(T param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func_for_cx</span><span class="params">(<span class="keyword">const</span> T param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func_for_rx</span><span class="params">(<span class="keyword">const</span> T&amp; param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> x = <span class="number">27</span>;</span><br><span class="line"></span><br><span class="line">	func_for_x(<span class="number">27</span>); <span class="comment">// 推导得出的param类型就是auto x = 27中x的类型</span></span><br><span class="line">	func_for_cx(x); <span class="comment">// 推导得出的param类型就是const auto cx = x中cx的类型，也就是const auto类型</span></span><br><span class="line">	func_for_rx(x); <span class="comment">// 同上类比</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最终的实际类型就是上面代码中param的实际类型。</p>
<p>我们首先规定如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(ParamType param)</span></span>;</span><br><span class="line"></span><br><span class="line">f(expr);</span><br><span class="line"></span><br><span class="line">ArgType x = expr;</span><br></pre></td></tr></table></figure>
<p>上面的代码中ArgType是一个带有auto的类型，最终ArgType和ParamType是等价的，而auto代表的类型其实就是T的类型。</p>
<p>所以在一般情况下，对于变量的类型推测，auto和C++11中template的类型推测方法是一样的，只要用如上的方式转化下就可以理解。</p>
<h1 id="auto对于函数、数组类型参数的推测"><a href="#auto对于函数、数组类型参数的推测" class="headerlink" title="auto对于函数、数组类型参数的推测"></a>auto对于函数、数组类型参数的推测</h1><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> name[] = <span class="string">"CrazySpy"</span>;</span><br><span class="line"><span class="keyword">auto</span> arr1 = name; <span class="comment">// arr1's type is const char*</span></span><br><span class="line"><span class="keyword">auto</span>&amp; arr2 = name; <span class="comment">// arr2's type is const char (&amp;)[13]</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span>, <span class="keyword">double</span>)</span></span>;</span><br><span class="line"><span class="keyword">auto</span> func1 = foo; <span class="comment">// func1's type is void (*)(int, double)</span></span><br><span class="line"><span class="keyword">auto</span>&amp; func2 = foo; <span class="comment">// func2's type is void (&amp;)(int, double)</span></span><br></pre></td></tr></table></figure>
<p>使用auto对于变量类型推测时的理解方法来看，auto在对于函数、数组类型参数的推测还是遵循C++11中template的那一套来做的。</p>
<h1 id="auto推测与C-11中template推测的不同点"><a href="#auto推测与C-11中template推测的不同点" class="headerlink" title="auto推测与C++11中template推测的不同点"></a>auto推测与C++11中template推测的不同点</h1><p>auto与C++11中template推测不同的地方只有一个，那就是对于initializer_list对象的推测结果是不同的。</p>
<p>在C++11标准中，我们可以用如下的方法初始化一个变量:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> x1 = &#123;<span class="number">27</span>&#125;;</span><br><span class="line"><span class="keyword">int</span> x2&#123;<span class="number">27</span>&#125;;</span><br></pre></td></tr></table></figure>
<p>这种初始化方式是使用了C++11的新特性之一—initializer_list。然而这种方式在使用auto进行类型推测时，却是和C++11中的template推测结果不一样。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f1</span><span class="params">(T param)</span></span>;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f2</span><span class="params">(<span class="built_in">initializer_list</span>&lt;T&gt; initList)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> x1 = &#123;<span class="number">11</span>,<span class="number">5</span>&#125;; <span class="comment">// x1 is initializer_list&lt;int&gt;</span></span><br><span class="line"></span><br><span class="line">f1(&#123;<span class="number">11</span>,<span class="number">5</span>&#125;); <span class="comment">//error, cannot deduce type!</span></span><br><span class="line">f2(&#123;<span class="number">11</span>,<span class="number">5</span>&#125;); <span class="comment">// T is int, initList is initializer_list&lt;int&gt;</span></span><br></pre></td></tr></table></figure>
<p>可以看出对于initializer_list类型来说，auto是自动推测成initializer_list类型，而template推测时，如果形参不是initializer_list<T>，则推测失败，否则T就为initializer_list中元素的类型。</p>
<h1 id="其它杂项"><a href="#其它杂项" class="headerlink" title="其它杂项"></a>其它杂项</h1><p>在C++11标准中：<br>auto不可以直接作为函数的返回值类型。<br>例如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在C++11标准中，这是不可以的</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">createInitList</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在lambda表达式的参数表当中也不允许参数为auto。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//在C++11标准中这是不可以的，c++14中可以</span></span><br><span class="line"><span class="keyword">auto</span> resetV = [&amp;v](<span class="keyword">const</span> <span class="keyword">auto</span>&amp; newVal) &#123;v = newVal;&#125;;</span><br></pre></td></tr></table></figure>
<p>五.总结</p>
<p>记住以下两点：</p>
<p>auto的类型推测结果和template类型推测结果是一样的，但是auto类型会将initializer_list推测为initializer_list类型，而template不会。<br>在C++11标准中，auto不可作为lambda的参数类型以及函数的返回值类型。但在C++14中可以，后面会具体出博客讲到。</p>
]]></content>
  </entry>
  <entry>
    <title>C++11标准的模板类型推测</title>
    <url>/2018/09/14/C++11%E6%A0%87%E5%87%86%E7%9A%84%E6%A8%A1%E6%9D%BF%E7%B1%BB%E5%9E%8B%E6%8E%A8%E6%B5%8B/</url>
    <content><![CDATA[<p>C++11标准中新添加的重要功能之一就是引入了&amp;&amp;，也就是称为右值(rvalue)的东西。右值的概念不再赘述，自行参考它处。<br>而在C++11的模板中，&amp;&amp;符号有不同的意思，因为它可能代表的不是右值。<br><a id="more"></a><br>有如下四种模板函数：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f1</span><span class="params">(<span class="keyword">const</span> T&amp; param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f2</span><span class="params">(T&amp;&amp; param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f3</span><span class="params">(T param)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f4</span><span class="params">(<span class="keyword">const</span> T param)</span></span>;</span><br></pre></td></tr></table></figure><br>模板类型推测就是推测不同类型的param在上面函数中作为参数时的类型推测的结果。书中将param分为：</p>
<ol>
<li>param为指针或者引用的，但不是全体引用(Universal Reference)。</li>
<li>param为引用，并且为全体引用，也就是“&amp;&amp;”。</li>
<li>param既不是指针也不是引用。<br>关于全体引用就是说它可以是左值引用，也可以是右值引用。这个是根据实际调用的传进来的参数决定的。它的表现形式是使用“&amp;&amp;”来表示的，它在template中不表示右值引用。具体的全体引用部分可以参看：<a href="../区别全体引用和右值引用">区别全体引用和右值引用</a></li>
</ol>
<p>接下来就来讨论这三种情况在实参类型不同时的类型推测，也就是T代表什么类型，param是什么类型。</p>
<p>我们可以利用编译器的错误提示来查看类型推测的结果:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> x = <span class="number">10</span>;</span><br><span class="line">	<span class="keyword">const</span> <span class="keyword">int</span> cx = x;</span><br><span class="line">	<span class="keyword">const</span> <span class="keyword">int</span>&amp; rx = cx;</span><br><span class="line"></span><br><span class="line">	f1(x); <span class="comment">// T is int, param is int&amp;;</span></span><br><span class="line">	f1(cx); <span class="comment">// T is const int, param is const int&amp;</span></span><br><span class="line">	f1(rx); <span class="comment">// T is const int, param is const int&amp;</span></span><br><span class="line"></span><br><span class="line">	f2(x); <span class="comment">// T is int&amp;, param is int&amp;</span></span><br><span class="line">	f2(cx); <span class="comment">// T is const int&amp;, param is const int&amp;</span></span><br><span class="line">	f2(rx); <span class="comment">// T is const int&amp;, param is const int&amp;</span></span><br><span class="line">	f2(<span class="number">10</span>); <span class="comment">// T is int, param is int&amp;&amp;</span></span><br><span class="line"></span><br><span class="line">	f3(x); <span class="comment">// T is int, param is int</span></span><br><span class="line">	f3(cx); <span class="comment">// T is int, param is int</span></span><br><span class="line">	f3(rx); <span class="comment">// T is int, param is int</span></span><br><span class="line">	f3(<span class="number">10</span>); <span class="comment">// T is int, param is int</span></span><br><span class="line"></span><br><span class="line">	f4(x); <span class="comment">// T is int, param is int</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>上面的代码编译后错误告诉了我们推测的类型结果，我把结论写在注释里了。</p>
<p>对于T的类型推测有明确的规律：</p>
<ul>
<li>param是一个指针或者引用，并且不是全体引用时：<ul>
<li>如果实参是一个引用，忽略实参的引用部分。然后根据实参类型和形参类型来决定T的类型，再根据T的类型决定param的类型。例如形参为T&amp;，实参为const int时，T是const int类型，param就是const int&amp;类型，这样带入也是符合C++的函数调用规律。</li>
</ul>
</li>
<li>param是一个全体引用时：<ul>
<li>如果实参是左值，T和param类型都为左值引用类型。这也是template中唯一推断类型为引用的情况。其次根据上面全体引用的介绍，也可以知道，这里的推断出param的类型不一定是右值引用，也有可能是左值引用，因为这里的&amp;&amp;不是右值引用的意思，而是全体引用！</li>
<li>如果实参是一个右值，那么就推导T为右值类型，这个按照常理来。</li>
</ul>
</li>
<li>param不是引用或者指针时：<ul>
<li>如果实参是一个引用，忽略实参的引用部分。</li>
<li>如果实参类型是const或者volatile的，忽略这两种关键字。</li>
<li>如果实参是数组名，那么就会把T推断为指向数组第0位的指针。如果想让T推断为一个数组时，可以设置param的类型为T&amp;。<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(T&amp; param)</span></span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> name[] = <span class="string">"hello world"</span>;</span><br><span class="line">f(name);</span><br></pre></td></tr></table></figure>
如上编写代码，T就为const char[12]，param为const char (&amp;) [12]。对于函数作为形参的情况也类似。</li>
</ul>
</li>
</ul>
<p>最后进行总结：<br>推断T时，</p>
<ul>
<li>当param是全体引用，而实参为左值时，T为左值引用类型。</li>
<li>当param中没有”&amp;”或者”*”时，为传值传参，实参的const和voliate特性不保留</li>
<li>当实参为数组名或者函数名时，T类型会退变成指针类型，指向数组或者函数的开头地址。除非param的类型为引用类型。</li>
</ul>
]]></content>
      <categories>
        <category>C/C++</category>
        <category>Effective Modern C++</category>
      </categories>
  </entry>
  <entry>
    <title>A*算法</title>
    <url>/2018/08/10/A*%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>poj有一道非常经典的题目:Knight Moves。大意是说每次移动“日”字型，最少移动多少次能达到目的地。学过算法的人都知道这是一题非常简单的bfs题。</p>
<p>今天我接触到了在hduoj上接触到了1560题。大意是要求一个最短序列，这个序列的子序列包含了给定的几个序列。</p>
<p>一开始我是想用暴力搜索结合剪枝的。题目中给定的子序列个数最多8个，每个序列最大长度为5，字符串由4中字符组成。这样所求的序列最长为40，如果用暴力搜索可能会达到<script type="math/tex">4^{40}</script>次，这个预期太可怕了（虽然剪枝会减少这一数字）。后来discuss中提示，使用A*算法。<br>A*算法在以前的接触当中更多的是用在路径搜索上，例如上面的poj的那道题。没有想到A*算法是可以用在这道hduoj题上的。</p>
<a id="more"></a>
<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>首先我们来回顾一下A*算法。为了相对准确表述，我对维基百科相关词条进行翻译，词条地址：A* search algorithm</p>
<p>A*算法又被称作为最好优先算法(best-first search)，这意味着这个算法通过在所有路径中寻找最少消耗的路径来解决问题，这个最少消耗可能是最短的路径、最少的时间等等。它最先考虑可能是最少消耗路径的路径，这是使用权重图(weighted graph)的数据结构计算得出的，大概的算法步骤：从图的某一个指定节点出发，根据下一步能走的节点构建一个路径树，然后下一步选择其中预计最优的节点走下去，重复构建这种路径树，直到其中某条路径能走到预先设定好的那个目标节点。</p>
<p>在每次的主循环迭代中，A*算法需要决定下一步走到哪个节点。这个在bfs(广度优先)算法中是不存在的。广度优先算法对于所有可以走的节点是一视同仁，而A*算法则是要考虑可能是最好路径一部分的那条路径。</p>
<p>A*算法的可能最优路径是通过如下公式得出的：</p>
<script type="math/tex; mode=display">f(n) = g(n) + h(n)</script><p>n是图中的某一个节点坐标；g(n)是从起始节点到n节点的最少花费；h(n)是一个启发式值，估计了n到目标节点的最少花费。这个启发式函数是根据问题决定的，当问题是求最短路径时，启发函数必须是可接受的，这也就是说这个函数永远不会高估实际到目标节点的最小花费。</p>
<h1 id="举例"><a href="#举例" class="headerlink" title="举例"></a>举例</h1><p>可能上面概念性的表述有一些难以理解，下面通过一个实例讲述A*算法。</p>
<p>求起点到终点的最短距离。</p>
<p>下面例子中，❤️表示终点，红色方框表示已经走过的节点（bfs中，已经走过的节点就不要再走了，这里的道理和bfs中的一样，走过就不用再走了），绿色方框表示可以走的节点，黑色是障碍物。方框中三个数，第一个数是f(n)，第二个是g(n)，第三个是h(n)。h(n)采用的启发函数是到目标节点的曼哈顿距离，因为到目标节点的花费肯定大于等于曼哈顿距离。</p>
<p>起点是第四行第二列的节点。<br><img src="/images/A*算法/A-Star1.png" alt=""></p>
<p>因为起点右边的节点F(h)最小，也就意味着起点到这个节点的花费和预计到终点的花费之和最小，也就是预计走这个节点的路径花费最小，所以优先考虑这个节点。而实际上一眼可以看出最短路径不可能经过这个节点。<br><img src="/images/A*算法/A-Star2.png" alt=""></p>
<p>按照算法继续走下去，应该是这样的。<br><img src="/images/A*算法/A-Star3.png" alt=""><br><img src="/images/A*算法/A-Star4.png" alt=""></p>
<p>在上面一张图的状态下，f(n)最小的未走过的节点是第三行第一列的那个节点，所以算法选择其继续走下去。<br><img src="/images/A*算法/A-Star5.png" alt=""></p>
<p>通过走回路，可以确定一条最短路径。<br><img src="/images/A*算法/A-Star6.png" alt=""></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>附上一张维基百科的gif用以帮助理解。<br><img src="/images/A*算法/Astar_progress_animation.gif" alt=""></p>
<p>从图上可以看出A*算法在后期不太会向左上角（偏离目标节点的方向）进行搜索，这还是因为左上角方向会增大启发函数的值，导致算法不会偏向这边，而是选择右上角。</p>
<p>但是也可以看出这套算法的弱点，就是如果启发函数值，也就是h(n)和实际值偏差很大，甚至出现h(n)越小，实际到终点需要的花费越大的情况，会导致A*算法的效率低下的问题，这也是bfs算法在竞赛中仍被使用的原因，要根据情景合理使用。</p>
]]></content>
      <categories>
        <category>算法</category>
      </categories>
  </entry>
  <entry>
    <title>母函数</title>
    <url>/2018/06/04/%E6%AF%8D%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>在数学中，某个序列的母函数(Generating function，又称生成函数)是一种形式幂级数，其每一项的系数可以提供关于这个序列的信息。<br>使用母函数解决问题的方法称为母函数方法。母函数可分为很多种，包括普通母函数、指数母函数、L级数、贝尔级数和狄利克雷级数。对每个序列都可以写出以上每个类型的一个母函数。构造母函数的目的一般是为了解决某个特定的问题，因此选用何种母函数要看序列本身的特性和问题的类型。<br><a id="more"></a><br>在算法比赛中母函数通常解决类似如下的问题：给5张1元，4张2元，3张5元，要得到15元，有多少种组合？某些时候会规定至少使用3张1元、1张2元、0张5元。某些时候会规定有无数张1元、2元、5元。</p>
<p>首先构造如下的多项式乘积：</p>
<script type="math/tex; mode=display">
\begin{align}
&(x^{value[0] * min[0]} + x^{value[0] * (min[0] + 1)} + ... + x^{value[0] * max[0]}) \nonumber \\
*&(x^{value[1] * min[1]} + x^{value[1] * (min[1] + 1)} + ... + x^{value[1] * max[1]})\nonumber \\
&...\nonumber \\
*&(x^{value[N] * min[N]} + x^{value[N] * (min[N] + 1)} + ... + x^{value[N] * max[N]}) \nonumber 
\end{align}</script><p>其中value[i]表示该乘积表达式第i个因子的权重，对应于具体问题的每个物品的价值或者权重。min[i]表示该乘积表达式第i个因子的起始系数，对应于具体问题中的每个物品的最少个数，即最少要取多少个。max[i]表示该乘积表达式第i个因子的终止系数，对应于具体问题中的每个物品的最多个数，即最多要取多少个。x没有实际意义，可以理解为为了凑成母函数需要的幂级数而添加的。这样的多项式乘积求出来的式子中，均为<script type="math/tex">a*x^{k}</script>的形式，其中x还是无意义；a代表了能得到k权重的方法有a个。</p>
<p>举个例子：给1张1元，1张2元，3张5元，要得到18元，有多少种组合？</p>
<p>我们先构造多项式乘积：</p>
<script type="math/tex; mode=display">
\begin{aligned}
&(x^{1*0}+x^{1*1})*(x^{2*0}+x^{2*1})*(x^{5*0}+x^{5*1}+x^{5*2}+x^{5*3}) \nonumber \\
=&(1+x)*(1+x^{2})*(1+x^{5}+x^{10}+x^{15})\nonumber \\
=&1+x+x^{2}+x^{2}+x^{5}+x^{6}+x^{7}+x^{8}+x^{10}+x^{11}+x^{12}+x^{13}+x^{15}+x^{16}+x^{17}+x^{18} \nonumber 
\end{aligned}</script><p>因为<script type="math/tex">x^{18}</script>的系数为1，所以有一种组合。</p>
<p>具体到程序，我在网上找到了下面的模板：首先因为比较难直接文字说明，我就先说一下以下代码注释中数学术语的约定（毕竟不是数学专业，而且我语文也很差）。<script type="math/tex">(1+x^{2})*(1+x^{6}+x^{12})*(x^{6}+x^{12})</script>为例<script type="math/tex">(1+x^{2})</script>为一个项，<script type="math/tex">(1+x^{6}+x^{12})</script>又为另一个项。以此类推<script type="math/tex">(1+x^{6}+x^{12})</script>中，<script type="math/tex">x^{6}</script>为该项中的一个数，1为另一个数，以此类推。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//a为索引为指数的项的系数，b为中间结果。</span></span><br><span class="line"><span class="keyword">int</span> a[MAX],b[MAX];</span><br><span class="line"><span class="comment">//初始化a</span></span><br><span class="line"><span class="built_in">memset</span>(a,<span class="number">0</span>,<span class="keyword">sizeof</span>(a));</span><br><span class="line"><span class="keyword">int</span> n = <span class="number">10</span>; <span class="comment">//表示有几种因子，也就是说有几种物品</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> P = <span class="number">15</span>; <span class="comment">//P是可能的最大指数</span></span><br><span class="line">a[<span class="number">0</span>]=<span class="number">1</span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;=n;i++)<span class="comment">//循环每个因子，就是现在是第几项与之前的结果相乘</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">memset</span>(b,<span class="number">0</span>,<span class="keyword">sizeof</span>(b));</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j=<span class="built_in">min</span>[i];j&lt;=<span class="built_in">max</span>[i]&amp;&amp;j*value[i]&lt;=P;j++)<span class="comment">//在这个项里面的选了几个几次幂,换到题目里就是选了i种物品j个。所以实际增加的权重是value[i] * j</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> k=<span class="number">0</span>;k+j*value[i]&lt;=P;k++)<span class="comment">//上层循环选的j*value[i]次幂要和前几项的乘积的每个数都乘，k是之前结果中选择的指数</span></span><br><span class="line">            b[k+j*value[i]]+=a[k];<span class="comment">//k + j * value[i]是j*value[i]次幂和k次幂相乘后的指数，这个指数的系数是在前几项的乘积的相同指数的数的系数上加上a[k],因为还没有乘到的项中的数的系数都为1，所以只要加a[k](即使要加也是a[k]*1)</span></span><br><span class="line">    <span class="built_in">memcpy</span>(a,b,<span class="keyword">sizeof</span>(b));<span class="comment">//b赋值给a</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数论</category>
        <category>算法</category>
        <category>组合数学</category>
      </categories>
  </entry>
  <entry>
    <title>大数乘法</title>
    <url>/2018/06/03/%E5%A4%A7%E6%95%B0%E4%B9%98%E6%B3%95/</url>
    <content><![CDATA[<p>先贴题目：<a href="http://acm.hdu.edu.cn/showproblem.php?pid=1042" target="_blank" rel="noopener">http://acm.hdu.edu.cn/showproblem.php?pid=1042</a></p>
<p>题意比较简单，求N!，N可能达到10000。<br><a id="more"></a><br>大数和正常数的乘法，算法如下</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> result[<span class="number">100000</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">multiply</span><span class="params">(<span class="keyword">int</span> num,<span class="keyword">int</span> w)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">int</span> jin = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; w; ++i)&#123;</span><br><span class="line">		<span class="keyword">int</span> a = result[i];</span><br><span class="line">		<span class="keyword">int</span> t = num * a + jin;</span><br><span class="line">		result[i] = t % <span class="number">10</span>;</span><br><span class="line">		jin = t / <span class="number">10</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">while</span>(jin)&#123;</span><br><span class="line">		result[w] = jin % <span class="number">10</span>;</span><br><span class="line">		jin /= <span class="number">10</span>;</span><br><span class="line">		++w;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> w;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="built_in">memset</span>(result,<span class="number">0</span>,<span class="keyword">sizeof</span>(result));</span><br><span class="line">	result[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">int</span> N;</span><br><span class="line">	<span class="keyword">int</span> w = <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">while</span>(<span class="built_in">scanf</span>(<span class="string">"%d"</span>,&amp;N) != EOF)&#123;</span><br><span class="line">		multiply(N,w);</span><br><span class="line"></span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> i = w - <span class="number">1</span>; i &gt;= <span class="number">0</span>; --i)&#123;</span><br><span class="line">			<span class="built_in">printf</span>(<span class="string">"%d"</span>,result[i]);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"\n"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面算法中，result中存放了一个大数乘数的倒序，main中的N(multiply中的num)是存放了另一个大数，w是result的长度。这样可以进行大数和一个int的乘法。</p>
<p>上述乘法的算法非常有意思，和一般的竖式乘法有一点不一样，请仔细揣摩。</p>
<p>如果两个数都是大数，那么算法如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rev</span><span class="params">(<span class="keyword">char</span> *current)</span></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> len = <span class="built_in">strlen</span>(current);</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; len / <span class="number">2</span>; ++i)&#123;</span><br><span class="line">		<span class="keyword">char</span> t = current[i];</span><br><span class="line">		current[i] = current[len - <span class="number">1</span> - i];</span><br><span class="line">		current[len - <span class="number">1</span> - i] = t;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">char</span> temp[<span class="number">100000</span>];<span class="comment">//累加器，临时变量</span></span><br><span class="line"><span class="keyword">char</span> result[<span class="number">100000</span>];<span class="comment">//一个乘数（上次的计算结果）</span></span><br><span class="line"><span class="keyword">char</span> input[<span class="number">100000</span>];<span class="comment">//另一个乘数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">multiply</span><span class="params">(<span class="keyword">char</span> *num)</span></span>&#123;</span><br><span class="line">	<span class="built_in">memset</span>(temp,<span class="number">0</span>,<span class="keyword">sizeof</span>(temp));</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; (<span class="keyword">int</span>)<span class="built_in">strlen</span>(result); ++i)&#123;</span><br><span class="line">		<span class="keyword">int</span> a = result[i] - <span class="string">'0'</span>;</span><br><span class="line">		<span class="keyword">int</span> jin = <span class="number">0</span>;</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; (<span class="keyword">int</span>)<span class="built_in">strlen</span>(num); ++j)&#123;</span><br><span class="line">			<span class="keyword">int</span> b = num[j] - <span class="string">'0'</span>;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span>(temp[i + j] == <span class="string">'\0'</span>) temp[i + j] = <span class="string">'0'</span>;</span><br><span class="line">			<span class="keyword">int</span> mult = a * b + temp[i + j] - <span class="string">'0'</span> + jin;</span><br><span class="line"></span><br><span class="line">			temp[i + j] = mult % <span class="number">10</span> + <span class="string">'0'</span>;</span><br><span class="line">			jin = mult / <span class="number">10</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">int</span> p = <span class="built_in">strlen</span>(input) + i;</span><br><span class="line">		<span class="keyword">while</span>(jin)&#123;</span><br><span class="line">			temp[p++] = jin % <span class="number">10</span> + <span class="string">'0'</span>;</span><br><span class="line">			jin /= <span class="number">10</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">memcpy</span>(result,temp,<span class="keyword">sizeof</span>(temp));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="built_in">memset</span>(result,<span class="number">0</span>,<span class="keyword">sizeof</span>(result));</span><br><span class="line">	result[<span class="number">0</span>] = <span class="string">'1'</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">while</span>(<span class="built_in">scanf</span>(<span class="string">"%s"</span>,input) != EOF)&#123;</span><br><span class="line">		rev(input);</span><br><span class="line">		multiply(input);</span><br><span class="line">		<span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="built_in">strlen</span>(result) - <span class="number">1</span>; i &gt;= <span class="number">0</span>; --i)&#123;</span><br><span class="line">			<span class="built_in">printf</span>(<span class="string">"%c"</span>,result[i]);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"\n"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个算法是完全模拟竖式计算的。</p>
]]></content>
      <categories>
        <category>算法</category>
        <category>数论</category>
      </categories>
  </entry>
  <entry>
    <title>Eratosthenes筛法</title>
    <url>/2018/06/01/Eratosthenes%E7%AD%9B%E6%B3%95/</url>
    <content><![CDATA[<p>HDU-1019：<a href="http://acm.hdu.edu.cn/showproblem.php?pid=1019" target="_blank" rel="noopener">http://acm.hdu.edu.cn/showproblem.php?pid=1019</a></p>
<p>大意就是讲求几个数的最小公约数，保证结果在long int范围内。</p>
<p>原来的思路是使用</p>
<script type="math/tex; mode=display">
\begin{align}
LCM(\ LCM(a, \ b), \ c) &== \ LCM(\ a, \ b, \ c) \nonumber \\
LCM(a, b) &== a * b / GCD(a, b) \nonumber
\end{align}</script><p>这两个公式求出。但是考虑到LCM(a,b)中，虽然a和b是在long int 范围内，但是LCM(a,b)不一定在。如果用long long int，引入c，LCM(a,b,c)不一定在long long int内，所以这个方法放弃。<br><a id="more"></a></p>
<p>想到之前看到过一个Eratosthenes筛法，思路就是构建素数表，然后根据</p>
<script type="math/tex; mode=display">
\begin{align}
a&=p_{1}^{n_{a1}}p_{2}^{n_{a2}}p_{3}^{n_{a3}}...p_{m}^{n_{am}} \nonumber \\
b&=p_{1}^{n_{b1}}p_{2}^{n_{b2}}p_{3}^{n_{b3}}...p_{m}^{n_{bm}} \ \  (p_{1}，p_{2}...p_{m}均为素数) \nonumber \\
LCM(a,b)&=p_{1}^{max(n_{a1},n_{b1})}p_{2}^{max(n_{a2},n_{b2})}...p_{m}^{max(n_{am},n_{bm})} \nonumber
\end{align}</script><p>素数表的最优实现可以参考这篇博客：<a href="../素数表的最优实现">素数表的最优实现</a><br>这个原理得出LCM(a,b)的素因数分解结果，从而得出最后的结论。</p>
]]></content>
      <categories>
        <category>算法</category>
        <category>数论</category>
      </categories>
  </entry>
  <entry>
    <title>素数表的最优实现</title>
    <url>/2018/06/01/%E7%B4%A0%E6%95%B0%E8%A1%A8%E7%9A%84%E6%9C%80%E4%BC%98%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<p>对于素数表的构建，基本思路是：除了1以外的正整数的倍数都不是素数。反之同理，素数不可能是某两个非1整数的积。<br>所以有以下代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i &lt;= n; ++i) p[i] = <span class="literal">true</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i &lt;= n; ++i)</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> j = i * <span class="number">2</span>; j &lt;= n; j += i)</span><br><span class="line">p[j] = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure></p>
<p>然后进行优化:<br>1.i的枚举至多只需要到sqrt(n) + 1即可<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i &lt; <span class="built_in">sqrt</span>(n) + <span class="number">1</span>; ++i)</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> j = i * <span class="number">2</span>; j &lt;= n; j += i)</span><br><span class="line">p[j] = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure></p>
<p>2.如果某个数不是素数，那么它的倍数可以用它的素因数表示，所以非素数可以排除<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i &lt; <span class="built_in">sqrt</span>(n) + <span class="number">1</span>; ++i)</span><br><span class="line"><span class="keyword">if</span>(p[i]) <span class="keyword">for</span>(<span class="keyword">int</span> j = i * <span class="number">2</span>; j &lt;= n; j += i) p[j] = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure></p>
<p>3.j不必从i<em>2开始筛，在i=2时筛过;也不必在i</em>3时开始..;不必从i<em>(i-1)时开始；而是应该从i</em>i开始<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">2</span>; i &lt; <span class="built_in">sqrt</span>(n) + <span class="number">1</span>; ++i)</span><br><span class="line"><span class="keyword">if</span>(p[i]) <span class="keyword">for</span>(<span class="keyword">int</span> j = i * i; j &lt;= n; j += i) p[j] = <span class="literal">false</span>;</span><br></pre></td></tr></table></figure></p>
<p>经过三轮优化得到的代码是最优的素数表实现代码。</p>
]]></content>
      <categories>
        <category>算法</category>
        <category>数论</category>
      </categories>
  </entry>
  <entry>
    <title>ziplist的联级更新（cascade update）</title>
    <url>/2018/05/27/ziplist%E7%9A%84%E8%81%94%E7%BA%A7%E6%9B%B4%E6%96%B0%EF%BC%88cascade%20update%EF%BC%89/</url>
    <content><![CDATA[<p>前面在讲ziplist中压缩节点时，提到过其中有一个数据量叫prevlen，这个是存储了前一个节点的大小，它的长度是根据前一个节点实际大小。如果前一个节点大小在254位以下，则prevlen为1字节；如果前一个节点大小大于等于254位，则prevlen为5字节，并且第1字节数据为254，第2-5字节数据为实际大小。</p>
<p>另外ziplist还支持任意位置插入节点，这个是通过ziplistInsert函数实现的，具体可以自行研究其实现。</p>
<p>这时考虑一个极端情况：有一个压缩表有entry1,entry2,entry3节点，大小分别为250位，250位,250位。然后我在entry1前插入一个节点entry0，大小为254位。此时entry1的prevlen就要更新，从1位变成了5位，此时entry1的大小变成了254位。因为entry1大小的改变，entry2的prevlen也要改变，变成5位…如此连锁更新下去。这就叫ziplist的联级更新(cascade update)。<br>它的具体实现是在ziplistCascadeUpdate函数中，大概的流程就是判断当前节点的prevlen能不能放得下上一个节点的大小，能就退出联级更新，否则就进行扩大操作，直到最后一个节点。具体写法请自行查看相应代码。</p>
]]></content>
      <categories>
        <category>Redis源码分析</category>
        <category>ziplist</category>
      </categories>
  </entry>
  <entry>
    <title>Redis的ziplist数据结构</title>
    <url>/2018/05/27/Redis%E7%9A%84ziplist%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/</url>
    <content><![CDATA[<p>ziplist主要用于存储长度受到限制的字符串和大小受到限制的数字。<br>ziplist在源代码中没有直接的结构体定义，而是在ziplistNew函数中开辟了一组连续空间，并且对连续空间进行位级别的赋值，从而达到类似于结构体的功能。</p>
<p>ziplist数据结构：<br><img src="/images/Redis的ziplist数据结构/ziplist.png" alt=""><br><a id="more"></a><br>各个数据的含义：</p>
<ul>
<li>ZIPLIST_BYTES：一个4字节数据，用于存放压缩表字节数。</li>
<li>ZIPLIST_TAIL：最后一个entry距离表头的位置。如上图：head + ZIPLIST_TAIL = entry_tail</li>
<li>ZIPLIST_LENGTH：entry个数。因为只有2字节，当数值小于等于UINT16_MAX则代表真是的entry个数，否则需要遍历得出真实个数。</li>
<li>ZIP_END：表的尾，定值是255，用来遍历压缩表时作为结束标记。</li>
</ul>
<p>ziplist在源代码中也没有直接的结构体定义存储在压缩表中的entry结构(zlentry不是的)。而是在插入数据的时候进行开辟的相关操作。</p>
<p>根据ziplistInsert的定义，分析的出entry由以下的数据组成：</p>
<ul>
<li>prevlen：这个是前一个节点的长度，用于寻找某个节点的前节点。</li>
<li>encoding：编码，表示了数据类型。具体格式如下：<br><img src="/images/Redis的ziplist数据结构/ziplist_encoding.png" alt=""></li>
</ul>
<p>可以看到当entry存放字符串（字节数组）时，encoding可以反映出字符串的长度；entry存放整数时，可以反映整数的大小范围。</p>
<ul>
<li>content：存放了具体的数据。</li>
</ul>
]]></content>
      <categories>
        <category>Redis源码分析</category>
        <category>ziplist</category>
      </categories>
  </entry>
  <entry>
    <title>MySql中order表创建失败</title>
    <url>/2018/04/21/MySql%E4%B8%ADorder%E8%A1%A8%E5%88%9B%E5%BB%BA%E5%A4%B1%E8%B4%A5/</url>
    <content><![CDATA[<p>今天在试图创建order表的时候出现了问题，怎么都创建不成功。不是语法问题。</p>
<p>order是sql关键字，创建它的表要`符号</p>
]]></content>
      <categories>
        <category>SQL</category>
      </categories>
  </entry>
  <entry>
    <title>C++中signed和unsigned的大小比较</title>
    <url>/2018/04/15/C++%E4%B8%ADsigned%E5%92%8Cunsigned%E7%9A%84%E5%A4%A7%E5%B0%8F%E6%AF%94%E8%BE%83/</url>
    <content><![CDATA[<p>有这样的一段代码，请说出其运行结果：<D-r><br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="number">0x80000000</span> &gt; a)</span><br><span class="line">        <span class="built_in">cout</span> &gt;&gt; <span class="string">"hello\n"</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">cout</span> &gt;&gt; <span class="string">"111111\n"</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br><a id="more"></a><br>上面程序运行的结果是111111</p>
<p>对于十六进制的整数，如果字面量（即以unsigned int来看）超出了signed int的范围，编译器则尝试以unsigned int作为其类型。由于0x80000000转换成unsigned int在signed int的范围外，而它在unsigned int的范围内，所以将其当作unsigned int。<br>当unsigned和signed进行大小比较时，signed会转换成unsigned，这样a就会变成0xFFFFFFFF的unsigned的整数，这是一个很大的数字（2^32 - 1，是unsigned int的最大值），所以b&gt;a。&lt;/p&gt;</p>
]]></content>
      <categories>
        <category>C/C++`</category>
      </categories>
  </entry>
  <entry>
    <title>Mac系统GUI应用程序不读取环境变量</title>
    <url>/2018/04/14/Mac%E7%B3%BB%E7%BB%9FGUI%E5%BA%94%E7%94%A8%E7%A8%8B%E5%BA%8F%E4%B8%8D%E8%AF%BB%E5%8F%96%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<p>前几天brew upgrade指令把系统里的应用更新了一遍，其中包括python3。后来发现Macvim打不开，有闪退情况，用terminal看了下错误，我安装了YouCompleteMe插件，这个插件是基于python的，而python的encoding包找不到，根据网上的说法是brewhome里新的python3没有配置环境变量，于是把PYTHONPATH和PYTHONHOME环境变量在.zshrc（我用的是zsh）里面配了以下，结果发生了很诡异的事情，在terminal中可以打开Macvim，而我在finder中打开它就闪退，当时Google百度都没相关案例，只能先搁置。<br><a id="more"></a><br>今天想到了个方案，我先把youcompleteme禁用了，然后发现terminal和finder中都能打开，我觉得可能是finder中打开Macvim没有读取到上面设置的两个环境变量。然后分别在两种情况下打开的Macvim中用命令行echo $PYTHONPATH，发现确实是如我上面的猜测，PYTHONPATH仅仅在terminal打开的情况下能读取到。知道问题就Google解决方案了。<br>然后我用的是macOS High Sierra，网上说的什么 /etc/launchd.conf在10.10的时候就没了，最后用了下面的命令解决的</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">launchctl setenv 环境变量名 环境变量内容</span><br></pre></td></tr></table></figure>
<p>例如我的系统就设置</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">launchctl setenv PYTHONPATH /usr/<span class="built_in">local</span>/Cellar/python/3.6.5/Frameworks/Python.framework/Versions/3.6/bin</span><br></pre></td></tr></table></figure>
<h1 id="重启不失效的设置方法"><a href="#重启不失效的设置方法" class="headerlink" title="重启不失效的设置方法"></a>重启不失效的设置方法</h1><p>自Mountain Lion以来，之前原有的environment.plist的方法已经确定失效了。新的有效的方法是在/etc/launchd.conf中添加指令设置环境变量。<br>这个文件默认并不存在，可以自己建立。其内容则与launchctl执行的指令的格式相同。如上述设置可在/etc/launchd.conf中添加如下内容：<br>setenv JAVA_VERSION 1.6<br>需要注意的是，在launchd.conf中的命令有一些限制。首先是不能进行环境变量的引用。比如再设置JAVA_HOME环境变量的话，不能使用类似/System/Library/Frameworks/JavaVM.Framework/$JAVA_VERSION/Home的方式来引用之前设置的JAVA_VERSION环境变量，必须明确写出1.6。同时环境变量的内容中也不允许出现空格。<br>launchd.conf仅在系统启动时被调用，因此修改完成后需要重启才能看到效果。<br>此方法转载自:<a href="http://zodiacg.net/2013/05/set-path-under-mac/" target="_blank" rel="noopener">http://zodiacg.net/2013/05/set-path-under-mac/</a></p>
]]></content>
      <categories>
        <category>MacOS</category>
      </categories>
  </entry>
  <entry>
    <title>跳跃表数据结构分析</title>
    <url>/2018/04/14/%E8%B7%B3%E8%B7%83%E8%A1%A8%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<p>Redis中的跳跃表数据结构是通过两个结构体实现的：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* ZSETs use a specialized version of Skiplists */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> &#123;</span></span><br><span class="line">	sds ele; <span class="comment">/* 节点存放的数据 */</span></span><br><span class="line">	<span class="keyword">double</span> score; <span class="comment">/* 节点排序依据 */</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">backward</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zskiplistLevel</span> &#123;</span></span><br><span class="line">		<span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">forward</span>;</span></span><br><span class="line">		<span class="keyword">unsigned</span> <span class="keyword">int</span> span;</span><br><span class="line">	&#125; level[]; <span class="comment">/* 节点的层数 */</span></span><br><span class="line">&#125; zskiplistNode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplist</span> &#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">header</span>, *<span class="title">tail</span>;</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> length;</span><br><span class="line">	<span class="keyword">int</span> level; <span class="comment">/* 表最大层数 */</span></span><br><span class="line">&#125; zskiplist;</span><br></pre></td></tr></table></figure><br>zskiplist结构体是跳跃表整体的数据结构，它里面存储了跳跃表的头和尾，以及跳跃表的最大长度（节点个数）和节点的最大层数。<br><a id="more"></a></p>
<p>zskiplistNode结构体则是跳跃表节点结构体。ele存储数据；score存储节点排序依据；backward则是存放了上一个节点（不一定是前驱，只是score小于并最接近当前节点，或者是score等于当前节点而ele小于并最接近当前节点的ele）；后驱结构体level，用于存储所有的后驱，分层存储，并且存储了当前节点的当前层到下一个当前层的节点的跨度。</p>
<p>我们用一张图来展示跳跃表的结构：<br><img src="/images/跳跃表数据结构分析/zskiplist.png" alt=""><br>这是一个length为10，level为4的跳跃表。<br>由上图可以看到，表的头结点不存储数据，Redis中的跳跃表也是如此，另外尾节点始终指向空。</p>
<p>另外，根据Redis对于跳跃表的Create函数（zslCreate函数）可以得知，每个节点最大拥有ZSKIPLIST_MAXLEVEL（32）层，所以这避免了跳跃表无限制增加层数的问题。</p>
]]></content>
      <categories>
        <category>Redis源码分析</category>
        <category>zskiplist</category>
      </categories>
  </entry>
  <entry>
    <title>C++的左移和右移</title>
    <url>/2018/04/12/C++%E7%9A%84%E5%B7%A6%E7%A7%BB%E5%92%8C%E5%8F%B3%E7%A7%BB/</url>
    <content><![CDATA[<p>今天在做《深入理解计算机系统》时有这样一道题</p>
<p>Problem 2.23<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun1</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">word</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span> (<span class="keyword">int</span>)((<span class="keyword">word</span> &lt;&lt; <span class="number">24</span>) &gt;&gt; <span class="number">24</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun2</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">word</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span> ((<span class="keyword">int</span>) <span class="keyword">word</span> &lt;&lt; <span class="number">24</span>) &gt;&gt; <span class="number">24</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>求w=0x000000C9时fun1(w)与fun2(w)<br><a id="more"></a></p>
<p>首先w先化成0000 0000 0000 0000 0000 0000 1100 1001<br>然后fun1(w)没什么问题，答案还是0x000000C9</p>
<p>fun2(w)的答案让我有一些意外。<br>(int)w &lt;&lt; 24 = 1100 1001 0000 0000 0000 0000 0000 0000(B) 然后再&gt;&gt;24，原本以为是和左移类似，将每一位向右移，补0，这样的话得到的是0000 0000 0000 0000 0000 0000 1100 1001，也就是0x000000C9。结果错的，答案是0xFFFFFFC9。</p>
<p>考虑了下，发生这种情况是因为C/C++中左移是逻辑左移，而右移不是逻辑右移，是算数右移。<br>这样算出来应该是1111 1111 1111 1111 1111 1111 1100 1001也就是0xFFFFFFC9</p>
<p>接下来简单说一下逻辑右移和算数右移的区别，逻辑右移就是单纯高位补0。而算数右移要看符号位，符号位是1则每次右移补1，符号位为0则每次右移补0，unsigned因为没有符号位（或者理解成符号位始终为0），所以unsigned类型的逻辑左移和逻辑右移始终补0。<br>那上面的例子，原来w是unsigned类型的，因为转换成了signed int了，所以是有符号位，而且&lt;&lt;24操作使得符号位为1，所以每次右移补符号位的1。</p>
]]></content>
      <categories>
        <category>C/C++</category>
      </categories>
  </entry>
  <entry>
    <title>size_t类型导致的异常</title>
    <url>/2018/04/12/size_t%E7%B1%BB%E5%9E%8B%E5%AF%BC%E8%87%B4%E7%9A%84%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<p>《深入理解计算机系统》有这样一道题:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">strlonger</span><span class="params">(<span class="keyword">char</span> *s, <span class="keyword">char</span> *t)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">strlen</span>(s) - <span class="built_in">strlen</span>(t) &gt; <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>这是一个用来判断s是否比t字符串长的一个函数。但是这个函数是有bug的。<br><a id="more"></a></p>
<p>第一眼我是看不出bug在哪里，后来看了解释：<br>strlen(char *s)这个函数的返回值是size_t类型的，size_t是一种unsigned int，所以当strlonger中的s小于t时，它们相减值也是unsigned，这样只有当两者长度相等时，strlonger函数才可能返回0。</p>
]]></content>
      <categories>
        <category>C/C++</category>
      </categories>
  </entry>
  <entry>
    <title>sds数据结构分析</title>
    <url>/2018/04/10/sds%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<p>sds是redis中用来存储动态字符串的数据结构。<a id="more"></a>其利用了五种容量不一的数据结构存储字符串数据。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">sdshdr5</span> &#123;</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">char</span> flags; <span class="comment">/* 3 lsb of type, and 5 msb of string length */</span></span><br><span class="line">	<span class="keyword">char</span> buf[];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">sdshdr8</span> &#123;</span></span><br><span class="line">	<span class="keyword">uint8_t</span> len; <span class="comment">/* used */</span></span><br><span class="line">	<span class="keyword">uint8_t</span> alloc; <span class="comment">/* excluding the header and null terminator */</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">char</span> flags; <span class="comment">/* 3 lsb of type, 5 unused bits */</span></span><br><span class="line">	<span class="keyword">char</span> buf[];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">sdshdr16</span> &#123;</span></span><br><span class="line">	<span class="keyword">uint16_t</span> len; <span class="comment">/* used */</span></span><br><span class="line">	<span class="keyword">uint16_t</span> alloc; <span class="comment">/* excluding the header and null terminator */</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">char</span> flags; <span class="comment">/* 3 lsb of type, 5 unused bits */</span></span><br><span class="line">	<span class="keyword">char</span> buf[];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">sdshdr32</span> &#123;</span></span><br><span class="line">	<span class="keyword">uint32_t</span> len; <span class="comment">/* used */</span></span><br><span class="line">	<span class="keyword">uint32_t</span> alloc; <span class="comment">/* excluding the header and null terminator */</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">char</span> flags; <span class="comment">/* 3 lsb of type, 5 unused bits */</span></span><br><span class="line">	<span class="keyword">char</span> buf[];</span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> __<span class="title">attribute__</span> ((__<span class="title">packed__</span>)) <span class="title">sdshdr64</span> &#123;</span></span><br><span class="line">	<span class="keyword">uint64_t</span> len; <span class="comment">/* used */</span></span><br><span class="line">	<span class="keyword">uint64_t</span> alloc; <span class="comment">/* excluding the header and null terminator */</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">char</span> flags; <span class="comment">/* 3 lsb of type, 5 unused bits */</span></span><br><span class="line">	<span class="keyword">char</span> buf[];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>五种数据结构大同小异。除了sdshdr5以外都有len、alloc、flags以及一个buf数组。</p>
<p>除去sdshdr5的其余四种数据结构，每种数据结构的前三个成员变量包含了数据的信息，称之为header。<br>根据注释可以很容易明白：len是结构体中buf的已用长度；alloc是申请的buf的空间大小；flags的低三位存放了sds的类型，高五位则是未使用的空间，没有任何的意义。flags的低三位的数据与SDS_TYPE_MASK（值为7）的与运算可以得知当前sds的类型，具体什么类型参见下方宏定义：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SDS_TYPE_5 0</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SDS_TYPE_8 1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SDS_TYPE_16 2</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SDS_TYPE_32 3</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SDS_TYPE_64 4</span></span><br></pre></td></tr></table></figure></p>
<p>对于sdshdr5的数据结构，它的flags成员变量的低三位全为空，用来与SDS_TYPE_MASK（值为7）的与运算得到0来表示这是一个sdshdr5的数据结构，高五位却是有意义的，意义和其它数据结构的len是一样的，用来表示buf的已使用长度。</p>
]]></content>
      <categories>
        <category>Redis源码分析</category>
        <category>sds</category>
      </categories>
  </entry>
  <entry>
    <title>dictScan函数分析</title>
    <url>/2018/04/08/dictScan%E5%87%BD%E6%95%B0%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<p>dictScan的定义如下:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">dictScan</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">		dict *d,</span></span></span><br><span class="line"><span class="function"><span class="params">		<span class="keyword">unsigned</span> <span class="keyword">long</span> v,</span></span></span><br><span class="line"><span class="function"><span class="params">		dictScanFunction *fn,</span></span></span><br><span class="line"><span class="function"><span class="params">		dictScanBucketFunction* bucketfn,</span></span></span><br><span class="line"><span class="function"><span class="params">		<span class="keyword">void</span> *privdata)</span></span></span><br></pre></td></tr></table></figure>
<p>这里的扫描分为两个步骤，第一步对桶执行用户自定义函数(bucketfn)，再对桶内每个节点执行用户自定义函数(fn)。</p>
<p>当进行dictScan时没有进行rehash，那么只要先对0号表的需要scan的桶进行用户自定义的操作，然后对于桶内节点进行遍历执行自定义函数即可。</p>
<p>如果正在进行rehash，这时候会比较复杂。<br><a id="more"></a><br>正在rehash时的scan代码：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">t0 = &amp;d-&gt;ht[<span class="number">0</span>];</span><br><span class="line">t1 = &amp;d-&gt;ht[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Make sure t0 is the smaller and t1 is the bigger table */</span></span><br><span class="line"><span class="keyword">if</span> (t0-&gt;<span class="built_in">size</span> &gt; t1-&gt;<span class="built_in">size</span>) &#123; <span class="comment">//这样可以把哈希表扩大和缩小统一看成扩大</span></span><br><span class="line">	t0 = &amp;d-&gt;ht[<span class="number">1</span>];</span><br><span class="line">	t1 = &amp;d-&gt;ht[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">m0 = t0-&gt;sizemask;</span><br><span class="line">m1 = t1-&gt;sizemask;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Emit entries at cursor */</span></span><br><span class="line"><span class="keyword">if</span> (bucketfn) bucketfn(privdata, &amp;t0-&gt;table[v &amp; m0]);</span><br><span class="line">de = t0-&gt;table[v &amp; m0];</span><br><span class="line"><span class="keyword">while</span> (de) &#123;</span><br><span class="line">	next = de-&gt;next;</span><br><span class="line">	fn(privdata, de);</span><br><span class="line">	de = next;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Iterate over indices in larger table that are the expansion</span></span><br><span class="line"><span class="comment"> * of the index pointed to by the cursor in the smaller table */</span></span><br><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">	<span class="comment">/* Emit entries at cursor */</span></span><br><span class="line">	<span class="keyword">if</span> (bucketfn) bucketfn(privdata, &amp;t1-&gt;table[v &amp; m1]);</span><br><span class="line">	de = t1-&gt;table[v &amp; m1];</span><br><span class="line">	<span class="keyword">while</span> (de) &#123;</span><br><span class="line">		next = de-&gt;next;</span><br><span class="line">		fn(privdata, de);</span><br><span class="line">		de = next;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* Increment bits not covered by the smaller mask */</span></span><br><span class="line">	v = (((v | m0) + <span class="number">1</span>) &amp; ~m0) | (v &amp; m0);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* Continue while bits covered by mask difference is non-zero */</span></span><br><span class="line">&#125; <span class="keyword">while</span> (v &amp; (m0 ^ m1));</span><br></pre></td></tr></table></figure></p>
<p>t0、t1指针指向0号和1号哈希表，但是t0指向0号和1号表里的大小较小的那个，t1执行另一个，具体原因下面分析。m0、m1指向t0、t1指向的哈希表的大小掩码(sizemask)。大小掩码根据分析dictht(dictHashTable)的数据结构可以知道，是哈希表可以存放的桶的数量-1。</p>
<p>dictht的数据结构如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">dictht</span> &#123;</span></span><br><span class="line">	dictEntry **table; <span class="comment">//指向表的第一个哈希桶，指向第一个桶的第一个节点</span></span><br><span class="line">	<span class="comment">/* 哈希表的情况 */</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="built_in">size</span>; <span class="comment">//桶的数量</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> sizemask; <span class="comment">//桶数量的掩码，掩码用于hashFunction(key)结果如果超过size，则将结果与sizemask做一个&amp;运算，这样将哈希结果框定到size内</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> used; <span class="comment">//节点的数量</span></span><br><span class="line">&#125; dictht;</span><br></pre></td></tr></table></figure>
<p>首先还是和没有rehash的操作一样，对t0指向的表(注意不一定是0号表。下面简称t0，t1类似)的指定桶(v号)执行用户自定义的桶操作，并且对桶内哈希节点进行用户自定义操作。</p>
<p>但是由于在rehash时有一部分数据可能在t1的相应的桶中，所以要对t1的相应的桶中的数据进行扫描，这样才能扫描完整。</p>
<p>t1表的扫描代码如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Iterate over indices in larger table that are the expansion</span></span><br><span class="line"><span class="comment"> * of the index pointed to by the cursor in the smaller table */</span></span><br><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">	<span class="comment">/* Emit entries at cursor */</span></span><br><span class="line">	<span class="keyword">if</span> (bucketfn) bucketfn(privdata, &amp;t1-&gt;table[v &amp; m1]);</span><br><span class="line">	de = t1-&gt;table[v &amp; m1];</span><br><span class="line">	<span class="keyword">while</span> (de) &#123;</span><br><span class="line">		next = de-&gt;next;</span><br><span class="line">		fn(privdata, de);</span><br><span class="line">		de = next;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* Increment bits not covered by the smaller mask */</span></span><br><span class="line">	v = (((v | m0) + <span class="number">1</span>) &amp; ~m0) | (v &amp; m0);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* Continue while bits covered by mask difference is non-zero */</span></span><br><span class="line">&#125; <span class="keyword">while</span> (v &amp; (m0 ^ m1));</span><br></pre></td></tr></table></figure>
<p>和之前一样，每次都要执行一个桶的自定义函数和并且对桶内节点扫描一遍，重点在于：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">v = (((v | m0) + <span class="number">1</span>) &amp; ~m0) | (v &amp; m0);</span><br></pre></td></tr></table></figure></p>
<p>在尝试理解这套算法之前，先明确以下的事实：</p>
<p>1.一个哈希节点的索引是由hashFunction(val) &amp; sizemask计算得出的，sizemask是为2^n-1。</p>
<p>2.hashFunction(val)&amp;m0 和 hashFunction(val)&amp;m1 的差别在高位，在m0的位数的范围内是一样的。 举个例子:<br>hashFunction(val) = 9(D) = 1001(B)<br>m0 = 3(D) = 11(B)<br>m1 = 15(D) = 1111(B)<br>hashFunction(val) &amp; m0 = 1001 &amp; 0011 = 0001(B) 即在t0表内索引是1(D)<br>hashFunction(val) &amp; m1 = 1001 &amp; 1111 = 1001(B) 即在t1表内索引是9(D)<br>它们在m0的位数(m0是2位)范围内是一样的，都是01。<br>由2可推得对于t0到t1是扩展的情况时，只要考虑m0位数+1~m1位数这个扩展位数的部分，枚举这段空间的0或1情况即可。<br>例如:<br>还是上述的条件，只要再考虑第3位(m0位数+1，m0两位)~第4位(m1位数)的枚举，即0001、0101、1001、1101在t1中的位置。</p>
<p>明确了上面的内容之后，这个算法的精妙之处就逐渐显现出来了。</p>
<p>上面算法的思路就是在v的[m0的位数+1,m1的位数]位枚举情况，而v的低m0的位不变。例如上面的例子，v初值为0001(B),低两位(m0的位数)不变，剩下的高两位进行枚举，根据上面的算法，v就得到0001(v初始值)、1001、0101、1101。<br>再由<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (v &amp; (m0 ^ m1))</span><br></pre></td></tr></table></figure><br>这行代码判断，v的[m0的位数+1,m1的位数]位的每一位是否都是0，如果是，那么就相当于t1表已经扫描完了。还是上面的例子，当v枚举到了原点：0001的时候退出枚举。</p>
<p>接下来就要把下一个枚举的v计算出来，这时候就是v的[m0的位数+1,m1的位数]位不变，都置0，v的低m0的位数的几位执行倒转+1倒转回去的操作。这也是最后一部分代码所要表达的就是在m0的位数的范围内将当前的v倒转+1再倒转，找到下一个要在t0和t1都进行扫描的索引号。代码如下:<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Set unmasked bits so incrementing the reversed cursor</span></span><br><span class="line"><span class="comment"> * operates on the masked bits of the smaller table */</span></span><br><span class="line">v |= ~m0;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Increment the reverse cursor */</span></span><br><span class="line">v = rev(v);</span><br><span class="line">v++;</span><br><span class="line">v = rev(v);</span><br></pre></td></tr></table></figure><br>这里的rev函数就是上面所说的高低位互相调换的操作。</p>
<p>它的具体实现也贴出来：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">rev</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">long</span> v)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> s = <span class="number">8</span> * <span class="keyword">sizeof</span>(v); <span class="comment">// bit size; must be power of 2</span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> mask = ~<span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span> ((s &gt;&gt;= <span class="number">1</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">		mask ^= (mask &lt;&lt; s); v = ((v &gt;&gt; s) &amp; mask) | ((v &lt;&lt; s) &amp; ~mask);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> v;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>通过该算法，最后当v的位数大于m1的位数，相当于把0号表、1号表都扫描完了。这时v &amp; m1 == 0。</p>
<p>这个算法可以保证每个节点都被扫描到，有可能会有节点重复被访问，但是节点重复访问存在于rehash到更小的表时，这种情况在这套算法里是不存在的，因为这里手动将t0指向较小的表，这样始终就是rehash扩大的情况，不会存在重复访问的问题，这也是为什么上面要进行t0指向较小的表的原因。</p>
<p>rehash到小表会导致重复访问的例子：<br>sizemask(0号表) = 1111(B)<br>sizemask(1号表) = 111(B)<br>0号表有桶0000 1000<br>1号表的000桶至少会在扫描如上两个桶的时候都被访问到。</p>
]]></content>
      <categories>
        <category>Redis源码分析</category>
        <category>dict</category>
      </categories>
  </entry>
  <entry>
    <title>dict的rehash机制</title>
    <url>/2018/04/08/dict%E7%9A%84rehash%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>dict的rehash是用于将数据分布在更大或更小的哈希表内。<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> empty_visits = n*<span class="number">10</span>; <span class="comment">/* Max number of empty buckets to visit. */</span></span><br></pre></td></tr></table></figure><br>框定了这一次rehash的桶的数量，为用户自定义的n的10倍。这样做是防止单次rehash消耗太多时间，影响其他的操作，分次操作可以提高并发效率。<br>节点被rehash之后就会从0号表移除，并且添加到1号表相应的桶中。所以当0号表变成空的时候，所有数据都已经rehash到了1号表中，这样本轮rehash完成，返回0。否则就返回1，还需要下次rehash以完成本轮rehash。</p>
<a id="more"></a>
<p>rehash的条件：<br>在添加元素的dictAddRaw函数中调用的_dictKeyIndex函数中，会对是否满足rehash条件进行判断，并在如果需要rehash时进行rehash(调用_dictExpandIfNeeded函数)。<br>_dictExpandIfNeeded代码如下：<br><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Expand the hash table if needed */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> _dictExpandIfNeeded(dict *d)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="comment">/* Incremental rehashing already in progress. Return. */</span></span><br><span class="line">	<span class="keyword">if</span> (dictIsRehashing(d)) <span class="keyword">return</span> DICT_OK;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* If the hash table is empty expand it to the initial size. */</span></span><br><span class="line">	<span class="keyword">if</span> (d-&gt;ht[<span class="number">0</span>].<span class="built_in">size</span> == <span class="number">0</span>) <span class="keyword">return</span> dictExpand(d, DICT_HT_INITIAL_SIZE);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* If we reached the 1:1 ratio, and we are allowed to resize the hash</span></span><br><span class="line"><span class="comment">	 * table (global setting) or we should avoid it but the ratio between</span></span><br><span class="line"><span class="comment">	 * elements/buckets is over the "safe" threshold, we resize doubling</span></span><br><span class="line"><span class="comment">	 * the number of buckets. */</span></span><br><span class="line">	<span class="comment">//默认是即使dict_can_resize关闭，已使用桶的数量是桶数量的5倍进行强制rehash。如果没有关闭则是超过桶数量就进行rehash</span></span><br><span class="line">	<span class="keyword">if</span> (d-&gt;ht[<span class="number">0</span>].used &gt;= d-&gt;ht[<span class="number">0</span>].<span class="built_in">size</span> &amp;&amp;</span><br><span class="line">			(dict_can_resize ||</span><br><span class="line">			 d-&gt;ht[<span class="number">0</span>].used/d-&gt;ht[<span class="number">0</span>].<span class="built_in">size</span> &gt; dict_force_resize_ratio))</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">return</span> dictExpand(d, d-&gt;ht[<span class="number">0</span>].used*<span class="number">2</span>); <span class="comment">//扩展到已使用桶数量的两倍</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> DICT_OK;</span><br></pre></td></tr></table></figure></p>
<p>可以看到，正在rehash时则不进行是否需要rehash的判断。而如果0号哈希表数据为空，则将哈希表扩展成最小的尺寸（根据DICT_HT_INITIAL_SIZE的宏定义，最小尺寸为4）。如果开启了dict_can_resize，当0号表的平均桶内数据量为1时，就进行rehash。如果没有开启，则会根据dict_force_resize_ratio（默认为5）宏定义，进行强制哈希。<br>每次哈希将表容量*2，这样表的容量可以保证是2^n，对于dictScan有重要作用。关于这部分可以参考之前的文章。</p>
]]></content>
      <categories>
        <category>Redis源码分析</category>
        <category>dict</category>
      </categories>
  </entry>
</search>
